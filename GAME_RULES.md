# 틀린 그림 찾기 게임 — 규칙 및 사양 (Source of Truth)

이 파일은 게임 개발 전 기간 동안 지속적으로 참고/갱신하는 단일 진실 공급원(SoT)이다.
요구 사항은 총 5단계(요구사항 단계)로 순차 전달되며, 각 단계가 확정될 때마다 이 파일에 섹션을 추가한다.
기존에 확정된 내용은 이후 단계에서 명시적으로 변경 지시가 없는 한 유지한다.

**기술 스택**: 순수 HTML/CSS/JavaScript(바닐라, 프레임워크·빌드 도구 없음), 정적 파일 + `localStorage`만 사용, 백엔드 없음.

## 요구사항 진행 상태

- [x] 요구사항 1단계: 한 판의 시작–조작–성공/실패–재시작 흐름
- [x] 요구사항 2단계: 해상도·연속 입력·포커스·일시정지·10분 실행 견고성
- [x] 요구사항 3단계: 난이도 값 데이터 기반 튜닝(10회+10회 비교)
- [x] 요구사항 4단계: 현재 판 값/보존 기록 분리, 빈 값·손상값 안전 처리
- [x] 요구사항 5단계: 이벤트 효과 + 접근성 토글, 개인정보/비밀값 점검

---

## 요구사항 1단계

### 원문 (하는 일 / 완료 목표 / 통과 기준)

- 하는 일: 시작·조작·성공 또는 실패·다시 시작이 이어지는 한 판을 만든다.
- 완료 목표: 짧은 플레이 흐름과 화면 상태를 분명하게 한다.
- 첫 행동: 플레이어 행동·성공 조건·실패 조건을 한 개씩 정한다. 도형과 글자만으로 한 판을 끝까지 실행한다.
- 통과 기준
  - T02-C01 제출한 모든 URL(결과물·소스)은 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 새 시크릿 창에서 열린다.
  - T02-C03 게임 규칙이 공개 화면에 보인다.
  - T02-C04 핵심 조작이 공개 화면에 보인다.
  - T02-C05 현재 게임 상태가 공개 화면에 보인다.
  - T02-C06 핵심 조작 한 번은 상태 변화 한 번으로 반영된다.
  - T02-C07 한 판은 시작 뒤 30초 안에 성공 또는 실패 상태에 도달할 수 있다.
  - T02-C08 성공 뒤 다시 시작하면 현재 판 상태가 초기화된다.
  - T02-C09 실패 뒤 다시 시작하면 현재 판 상태가 초기화된다.
- 막히는 지점과 확인 순서: 조작이나 다시 시작이 반응하지 않는 경우 → 입력 초점 확인 → 이벤트 등록 횟수 확인 → 상태·타이머 초기화 확인
- 남길 것: 한 판의 시작·진행·종료·재시작

### 확정 사양 (Q&A로 확정)

#### 게임 개요
- 장르: 틀린 그림 찾기
- 전체 구성: 총 5단계(레벨), 단계가 올라갈수록 난이도 상승, 이전 단계를 성공해야 다음 단계로 진행 가능
- 비주얼 원칙: ~~도형(사각형/원/삼각형 등)과 글자만 사용, 이미지·사진 사용 안 함~~
  - ⚠️⚠️ 개정(최종, 이미지 자산 준비 완료 후 전면 교체): **이 원칙은 폐기되었다.** 1~5단계 전부 `assets/`의 실제 사진 위에서 다른점을 찾는 방식으로 완전히 대체됨. 도형 기반 렌더링 코드는 삭제되었으며 더 이상 폴백으로도 존재하지 않는다. 상세는 "이미지 기반 전환" 섹션 참고.
- ~~판 구성: 화면 좌/우 두 패널에 동일한 도형 세트를 대칭 배치. 정답(다른점) 도형은 위치 또는 색깔 중 하나만 다름, 모양·크기는 항상 동일하게 유지~~ (이미지 기반 전환으로 대체됨 — 아래 참고)
  - (참고용 이력) 요구사항 3단계에서: 최초 확정 시에는 "위치만 다름"이었으나, 색깔 차이도 정답 조건으로 추가 확정된 바 있음 (도형 기반 시절의 기록)
  - 각 다른점마다 위치차이/색깔차이 중 무엇이 적용될지, 그리고 위치차이의 이동 거리는 **정해진 무작위 범위 내에서 매 판마다 랜덤 결정**됨 (아래 "요구사항 3단계 > 난이도 랜덤화 원칙" 참조)

#### 시작(홈) 화면
- ⚠️ 개정(추가 요청으로 변경, 화면 분리): 홈 화면에는 **이름 입력**과 **순위 보기 링크**만 둔다. 게임 규칙·핵심 조작 안내문은 홈 화면에서 제거하고 아래 "규칙 확인 화면"으로 옮겼다.
- 이름 입력: **필수**. 입력 전에는 "게임 시작" 버튼 비활성화
  - 단순 텍스트 입력이며 계정 생성/로그인/OAuth가 아니므로 T02-C01 위반 아님
- 순위(랭킹) 페이지로 이동하는 링크 상시 노출
- "게임 시작" 버튼 클릭 시 곧바로 게임을 시작하지 않고, 아래 "규칙 확인 화면"으로 이동한다 (이름 유효성 검사는 기존과 동일하게 이 버튼에서 수행).
- ⚠️ 개정(추가 요청, 비주얼 스타일): 홈 화면은 회원가입 폼 느낌으로 배치한다 — 중앙 정렬된 카드 하나에 아바타 아이콘, "플레이어 등록" 타이틀, 이름 입력 필드, 큰 기본 버튼("게임 시작"), 그 아래 텍스트 링크 스타일 보조 버튼("순위 보기") 순서로 배치. **이것은 순전히 시각적 스타일 차용일 뿐, 실제 계정 생성·로그인·인증 기능은 전혀 추가하지 않는다** — 이름 입력은 여전히 단순 텍스트 필드이며 T02-C01 원칙(계정/로그인 없이 시크릿 창에서 바로 실행)은 그대로 유지된다.

#### 규칙 확인 화면 (⚠️ 추가 요청으로 신설)
- 홈 화면에서 "게임 시작"을 눌렀을 때 곧바로 게임(1단계)로 들어가지 않고, 이 중간 화면을 거친다. **새 게임을 시작할 때 딱 1회만** 나타나며, 이후 "다음 단계" 버튼으로 2단계 이상으로 넘어갈 때는 다시 보여주지 않는다 (스테이지 전환은 기존과 동일하게 단계 성공 오버레이 → "다음 단계" 버튼으로 처리).
- 화면에 표시하는 내용: 기존 홈 화면에 있던 "게임 규칙"과 "핵심 조작" 안내문 전체를 그대로 옮겨서 표시 (T02-C03, T02-C04 충족 — 게임 시작 직전 공개 화면에 규칙·조작법이 보임).
- "시작하기" 버튼을 눌러야 실제로 1단계 타이머가 시작되고 게임 화면으로 전환된다.

#### 접근성 토글 상시 노출 범위 (⚠️ 2026-09-17 개정: 화면별 배치 → 전역 고정 배치)
- 음소거/움직임 감소 토글은 더 이상 화면마다 따로 두지 않는다. **뷰포트 왼쪽 상단에 고정(`position: fixed`)된 컨트롤 하나**로 통합해, 홈/규칙 확인/게임/순위 어느 화면에 있든(스크롤·화면 전환과 무관하게) 항상 같은 자리에서 켜고 끌 수 있다.
- 각 버튼은 `<button aria-pressed>` 하나뿐이므로 화면 간 상태 동기화 로직 자체가 필요 없어졌다(기존에는 화면 3곳에 각각 있던 엘리먼트의 값을 서로 맞춰줘야 했음). 이벤트 리스너도 버튼당 정확히 1개(click)만 등록한다.
- 화면 전환/오버레이(일시정지·성공·실패)보다 높은 z-index로 항상 위에 표시되어, 오버레이가 떠 있는 동안에도 클릭할 수 있다.
- 이 컨트롤과 겹치지 않도록 `body`의 상단 여백을 확보해뒀다(요구사항 2단계 "해상도 대응" 섹션의 유동형 레이아웃 원칙과 동일하게 vh 단위로 처리, 상세 값은 코드 주석 참고).

#### 단계별 구성

| 단계 | 찾아야 할 다른 점 개수 | 제한시간 |
|---|---|---|
| 1 | 5개 | 3:00 |
| 2 | 5개 | 2:30 |
| 3 | 5개 | 2:00 |
| 4 | 5개 | 1:30 |
| 5 | 5개 | 1:00 |

#### 핵심 조작 (T02-C04, T02-C06)
- 좌/우 패널을 비교하여 다른 위치에 있는 도형을 클릭/탭
- 조작 1회 = 상태 변화 1회
  - 정답 클릭 → 찾은 개수 +1, 해당 도형에 정답 표시
  - 오답 클릭 → 오답 피드백 표시 + **제한시간에서 즉시 10초 차감** (아래 개정 참조)
- ⚠️ 개정(추가 요청으로 변경): 오답을 클릭할 때마다 남은 시간에서 10초를 즉시 차감한다. 차감 결과 남은 시간이 0 이하가 되면 그 즉시 실패 처리한다 (오답 발생도 T02-C06의 "조작 1회 = 상태 변화 1회"에 해당하는 상태 변화로 취급)

#### 성공/실패 조건
- 성공: 제한시간 내에 해당 단계의 다른 점 5개를 모두 찾음
- 실패: 다음 중 하나라도 발생하면 실패
  - 5개를 다 찾기 전에 해당 단계 제한시간(오답 차감 포함) 초과
  - 오답 클릭 누적으로 남은 시간이 0 이하가 됨 (10초씩 차감)
- 화면에 항상 표시되는 현재 상태 (T02-C05): 현재 단계 번호, 남은 시간(⚠️ 아래 "남은 시간 표시 방식" 참고), 찾은 개수/전체 개수

#### 남은 시간 표시 방식 (⚠️ 추가 요청으로 확정, 텍스트 → 막대 게이지로 변경)
- 상태 표시줄의 "남은시간 0:00" 숫자 텍스트는 제거하고, 대신 **점점 줄어드는 막대(게이지)** 하나로만 남은 시간을 표시한다.
- **위치**: 기준 이미지·오답 이미지 박스(`#boards`) 바로 아래.
- **길이(너비)**: 기준 이미지 박스의 왼쪽 시작점부터 오답 이미지 박스의 오른쪽 끝까지, 즉 `#boards` 전체 너비와 동일하게 꽉 채운다.
- **줄어드는 방식**: 막대는 항상 해당 단계의 정해진 제한시간(예: 1단계 3:00) 대비 **현재 남은 시간의 비율**만큼 채워진 상태로 표시된다 (`남은시간 ÷ 그 단계 제한시간`). 시간이 흐르거나 오답 클릭으로 10초가 차감될 때마다 이 비율에 맞춰 막대 길이가 즉시 줄어든다.
- 이 값은 기존에 `state.remaining`/`stageTimeLimit(state.stage)`로 이미 계산되는 값 그대로를 재사용하는 표시 방식 변경일 뿐이므로, 성공/실패/페널티 판정 로직 자체는 변경되지 않는다.

#### 단계 진행 / 재시작 흐름
- 1~4단계 성공 시: "다음 단계" 버튼 클릭으로 다음 단계 시작 (자동 전환 아님)
- 5단계(마지막) 성공 시: 자동으로 순위(랭킹) 페이지로 이동
- 어느 단계에서든 실패(시간 초과) 시: "다시 시작" 버튼 → **처음 화면(이름 입력/시작 화면)으로 완전히 리셋**, 진행 중이던 모든 단계 기록 초기화 (T02-C09)
- 성공 뒤 다시 시작해도 동일하게 현재 판 상태 초기화 (T02-C08)

#### 순위(랭킹) 페이지
- 로그인/계정 불필요 → T02-C01 준수
- 브라우저 `localStorage`에 이름 기반 기록 저장, 이전 기록과 비교하여 순위 표시 (서버/백엔드 없음)
- 접근 경로: (1) 홈 화면 링크로 언제든 접근 가능, (2) 5단계 클리어 시 자동 이동

### 통과 기준 → 설계 매핑 (자체 점검용)

- T02-C01: 이름 입력은 텍스트필드일 뿐 계정이 아니며, 랭킹도 로그인 없이 로컬(localStorage)에만 저장 → 시크릿 창에서 인증 없이 전체 플레이 가능
- T02-C03 / T02-C04: ⚠️ 개정(화면 분리 이후) 규칙 확인 화면(게임 시작 직전, 새 게임당 1회)에 규칙·조작법을 공개 표시하여 충족
- T02-C05: 게임 화면에 단계/남은시간/찾은개수 상시 표시
- T02-C06: 도형 클릭 1회 → 상태(찾은 개수 또는 오답 피드백) 1회 변화
- T02-C07: 단계별 제한시간은 1~3분이지만, **성공은 정답을 빠르게 모두 찾으면 30초 이내에도 도달 가능**하도록 설계 (조기 성공 종료를 막지 않음) → "30초 안에 성공 또는 실패 상태에 도달할 수 있다"는 조건을 성공 경로로 충족
- T02-C08 / T02-C09: 재시작은 항상 홈 화면으로 이동하며 모든 판 상태(단계, 타이머, 찾은 개수) 초기화

### 막힘 방지 체크리스트
- 조작/다시 시작 버튼이 반응하지 않을 때 확인 순서:
  1. 입력 초점(포커스) 확인
  2. 이벤트 리스너 중복 등록 여부 확인
  3. 상태값·타이머 초기화 여부 확인

---

## 요구사항 2단계

### 원문 (하는 일 / 완료 목표 / 통과 기준)

- 하는 일: 두 해상도와 연속 입력·포커스·일시정지·10분 실행을 검사한다.
- 완료 목표: 입력과 시간 상태가 화면 조건 변화에도 안정적으로 이어지게 한다.
- 첫 행동: 다섯 검사 범주에 예상값을 적는다. 두 해상도와 10분 실행까지 하나씩 수행한다.
- 통과 기준
  - T02-C10 1366×768에서 조작 영역 잘림과 가로 넘침이 0건이다.
  - T02-C11 1920×1080에서 조작 영역 잘림과 가로 넘침이 0건이다.
  - T02-C12 1초 안에 같은 핵심 입력 이벤트 10건을 보낸 검사에서 이벤트 한 건당 핵심 동작이 정확히 한 번 반영된다.
  - T02-C13 창 크기 변경 뒤 게임 상태와 조작이 유지된다.
  - T02-C14 포커스 이탈과 복귀 뒤 게임 상태와 조작이 유지된다.
  - T02-C15 일시정지 중 상태가 진행되지 않고 재개 뒤 이어진다.
  - T02-C16 10분 연속 실행 뒤 게임을 계속 조작할 수 있다.
  - T02-C17 10분 연속 실행 뒤 브라우저 콘솔 빨간 오류가 0건이다.
- 막히는 지점과 확인 순서: 복귀 직후 실패하거나 입력이 중복되는 경우 → 이탈 중 타이머 정지 여부 → 재개 기준 시각 → 이벤트 누적 여부
- 남길 것: PC 경계 검사표와 10분 콘솔 결과

### 확정 사양 (Q&A로 확정)

#### 일시정지 (T02-C15)
- 발동 방식: 게임 화면의 명시적 **"일시정지" 버튼** 클릭 (포커스 이탈에 의한 자동 일시정지는 아님)
- 일시정지 중: 제한시간 타이머 정지, 도형 클릭 등 핵심 입력 무시(상태 변화 없음)
- 재개("계속하기" 버튼) 시: 정지했던 시점의 남은 시간·찾은 개수 그대로 이어서 진행

#### 포커스 이탈/복귀 (T02-C14)
- 탭 전환, 창 비활성화 등 포커스 이탈과는 **무관하게 타이머는 계속 진행**한다 (자동 일시정지 없음)
- 포커스를 잃었다가 되찾아도 게임 상태(단계, 남은 시간, 찾은 개수, 도형 배치)와 조작 가능 여부는 그대로 유지되어야 한다
- 즉 "일시정지"는 오직 명시적 버튼으로만 제어되고, 포커스 이벤트는 상태에 어떤 부수효과도 일으키지 않아야 한다 (타이머 이중 감소, 이벤트 중복 등록 금지)

#### 해상도 대응 (T02-C10, T02-C11)
- 방식: **유동형 단일 레이아웃** (%, vw/vh 등 상대 단위 기반), 별도의 두 세트 레이아웃을 만들지 않음
- 1366×768과 1920×1080 두 해상도 모두에서 좌/우 게임 패널, 상태 표시줄(단계/시간/찾은개수), 버튼(일시정지/다시시작/다음 단계)이 잘리거나 가로 스크롤이 발생하지 않아야 함
- 고정 px 폭을 뷰포트보다 크게 주지 않고, 이미지 대신 도형(SVG/CSS)만 쓰므로 리사이즈에도 레이아웃 재계산이 쉬움을 활용

#### 이미지 박스 크기 통일 및 세로 스크롤 방지 (⚠️ 추가 요청으로 확정, 이미지 기반 전환 이후)
- 위 "해상도 대응" 원칙은 도형 기반 시절 기준이었고, 이미지 기반 전환 이후에는 이미지 세트마다 원본 해상도·가로세로 비율이 서로 크게 다르다(예: `image1`은 가로로 넓은 사진, `image2`/`image4`는 정사각형에 가까운 사진, `image3`은 세로로 긴 사진). 이 차이 때문에 "가로 폭 100%, 세로는 비율대로 자동"으로만 두면 세로로 긴 이미지가 배정된 단계에서 페이지 전체 높이가 뷰포트를 넘어 세로 스크롤이 생길 수 있었다. 아래 규칙으로 이를 막는다.
- **박스 크기 통일**: 좌(기준)/우(오답) 두 이미지가 그려지는 박스(`.board`)는 이미지의 원본 해상도·비율과 무관하게 항상 같은 크기를 갖는다. 박스 크기는 뷰포트(현재 창 크기)에서 상태 표시줄·토글 영역을 제외하고 남는 공간을 채우도록 계산되며, 어떤 이미지 세트가 배정되든(1~5단계 전부) 같은 방식으로 동일하게 적용된다.
- **세로 스크롤 방지**: 게임 화면(`#game-screen`) 전체 높이를 뷰포트 높이에서 페이지 상하 여백만큼 뺀 값으로 고정하고, 그 안에서 상태 표시줄·토글 영역은 필요한 만큼만 차지하고 보드 영역이 나머지 공간을 채우도록 배치한다. 어떤 이미지가 나오든 이 고정된 높이를 넘지 않으므로 게임 화면에서 세로 스크롤은 발생하지 않는다.
- **여백**: 페이지 상하 여백(홈/게임/순위 화면 공통으로 이미 적용 중인 바깥 여백)은 그대로 유지되어, 보드 영역이 브라우저 창 위/아래 끝에 딱 붙지 않고 항상 약간의 여백을 두고 배치된다.
- **비율 유지(찌그러짐/잘림 없음)**: 박스 크기는 고정이지만 그 안에 그려지는 이미지 자체는 원본 가로세로 비율 그대로 표시된다(늘리거나 찌그러뜨리지 않음). 박스 비율과 이미지 비율이 다르면 그 차이만큼 이미지 상하 또는 좌우에 약간의 여백이 생길 수 있는데, 이는 이미지를 자르거나 왜곡하지 않기 위해 의도된 동작이다. 클릭 판정 좌표(`diffs.js`의 비율 좌표)는 실제로 그려진 이미지 영역을 기준으로 정확히 환산해서 계산하므로, 이 여백 부분을 클릭해도 오답으로 처리되지 않고 무시된다.
- **해상도 변화 대응**: 창 크기가 바뀌면 위 박스 크기 계산이 자동으로 다시 이루어져 비율을 유지한 채 커지거나 작아진다. 이 과정은 CSS(뷰포트 단위·flex 레이아웃)만으로 처리되어, 기존 견고성 원칙(리사이즈는 레이아웃만 재계산하고 게임 상태는 건드리지 않음)을 그대로 따른다.

##### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C10 / T02-C11: 게임 화면 높이를 뷰포트 기준으로 고정하고 보드 영역이 남은 공간만 채우도록 하여, 세로로 긴 이미지가 나와도 잘림·세로 스크롤이 생기지 않음
- T02-C06: 이미지 박스에 여백(letterbox)이 생겨도 클릭 좌표를 실제 그려진 이미지 영역 기준으로 보정하므로, 여백을 클릭했을 때 잘못된 오답 판정(상태 변화)이 발생하지 않음

#### 연속 입력 처리 (T02-C12)
- 동일 핵심 입력(도형 클릭)이 1초 내 10건 들어와도 이벤트 리스너는 요소당 정확히 1개만 등록하여 각 클릭이 정확히 1회씩만 상태에 반영되어야 함 (중복 바인딩으로 인한 이중 반영 금지, 디바운스/스로틀로 인한 유실도 금지)
- 이미 정답 처리된 도형을 다시 클릭해도 추가 상태 변화(중복 카운트)는 발생하지 않음

#### 창 크기 변경 (T02-C13)
- 리사이즈 이벤트 자체는 게임 상태(단계, 타이머, 찾은 개수, 도형 배치)를 초기화하거나 변형시키지 않음
- 레이아웃만 유동형 규칙에 따라 재배치되고, 진행 중이던 판의 논리적 상태는 그대로 유지

#### 장시간 실행 (T02-C16, T02-C17)
- 10분 연속 실행(방치 또는 반복 플레이) 후에도 도형 클릭, 일시정지, 다시 시작 등 모든 조작이 정상 동작해야 함
- 10분 동안 setInterval/타이머 누적, 이벤트 리스너 누적 등으로 인한 메모리 누수나 콘솔 에러(빨간 오류)가 발생하지 않도록 구현 (컴포넌트/단계 전환 시 이전 타이머·리스너 정리 필수)

### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C10 / T02-C11: 유동형(%, vw/vh) 단일 레이아웃, 고정 최소 폭 금지, overflow 발생 요소 없음
- T02-C12: 요소당 이벤트 리스너 1개, 상태 업데이트는 멱등적으로 처리(이미 처리된 도형 재클릭 무시)
- T02-C13: 리사이즈 핸들러는 레이아웃(CSS)만 건드리고 게임 상태(state)는 건드리지 않음
- T02-C14: visibilitychange/blur/focus 이벤트에 타이머·상태를 연동하지 않음 (포커스와 무관하게 계속 진행)
- T02-C15: 일시정지 버튼으로만 타이머/입력을 멈추고, 재개 버튼으로만 다시 흐르게 함
- T02-C16 / T02-C17: 타이머·이벤트 리스너는 언마운트/단계전환 시 반드시 해제(cleanup)하여 누적 방지

### 막힘 방지 체크리스트
- 포커스 복귀 직후 실패하거나 입력이 중복되면 확인 순서:
  1. 포커스 이탈 중 타이머가 의도치 않게 멈췄는지 확인
  2. 재개 시 기준 시각(타임스탬프) 계산이 올바른지 확인
  3. 이벤트 리스너가 중복 누적되지 않았는지 확인

---

## 요구사항 3단계

### 원문 (하는 일 / 완료 목표 / 통과 기준)

- 하는 일: 난이도 값 하나만 바꾸고 같은 조건의 플레이 10회씩을 비교한다.
- 완료 목표: 느낌이 아니라 플레이 기록으로 최종값을 고른다.
- 첫 행동: 현재 설정으로 점수·시간·실패 원인을 10회 기록한다. 값 하나만 바꾸고 다시 10회 기록해 최종값을 선택한다.
- 통과 기준
  - T02-C18 변경 전 같은 설정으로 플레이한 기록이 10회 있다.
  - T02-C19 변경 후 같은 설정으로 플레이한 기록이 10회 있다.
  - T02-C20 변경 전후 두 묶음 사이에서 난이도 값 한 개만 바뀌었다.
  - T02-C21 최종 난이도 선택 이유가 기록의 범위·중앙값 또는 반복 실패 원인과 연결된다.
- 막히는 지점과 확인 순서: 여러 값을 함께 바꾼 경우 → 변경 전 복원 → 값 하나 선택 → 나머지 고정 뒤 재실행
- 남길 것: 20회 플레이표와 최종 선택 이유

### 확정 사양 (Q&A로 확정)

#### 난이도 랜덤화 원칙 (게임 디자인)
1단계의 "도형과 글자만" 원칙은 유지하되, 아래 요소들은 매 판(라운드)마다 정해진 범위/규칙 내에서 무작위로 결정되도록 게임을 설계한다:
- **개수**: 현재는 요구사항 1단계대로 단계당 5개로 고정 (이번 3단계 1차 테스트 대상 아님, 추후 별도 라운드에서 범위화 검토 — 백로그)
- **위치차이 범위**: 정답 도형이 원본 대비 얼마나 이동해 있는지의 픽셀 거리 범위. **이번 3단계 1차 테스트의 튜닝 대상**
- **색깔차이 규칙**: 다른점 중 색깔차이가 적용될 확률/색상 변경 폭. 현재는 임시 기본값 사용, 추후 별도 라운드에서 튜닝 (백로그)
- **제한시간**: 단계별 고정값(3:00/2:30/2:00/1:30/1:00) 그대로 계속 적용, 무작위화 대상 아님

즉, 게임 자체에는 무작위성이 있지만(매 판 배치가 달라짐), 3단계 튜닝 테스트에서는 **한 라운드당 무작위 범위/규칙 파라미터 1개만** 변경하고 나머지는 두 묶음(변경 전/후)에서 완전히 동일하게 고정한다 — 이렇게 해야 T02-C20을 지키면서도 "무작위 비교"를 할 수 있다.

#### 1차 테스트 라운드 설계 (대상: 요구사항 1단계, 즉 1단계 스테이지 단독)
- 튜닝 대상 파라미터: **위치차이 이동 거리 범위**
- 변경 전 설정(예시, 실측 시 실제 값으로 대체): 위치차이 범위 A (예: 5~10px)
- 변경 후 설정: 위치차이 범위 B (예: 15~20px)
- 두 묶음에서 동일하게 고정할 값: 개수(5개), 색깔차이 규칙(임시 기본값), 제한시간(3:00)
- 각 묶음 10회씩, 총 20회 플레이 기록

#### 기록표 항목 (20회 플레이표)
| 회차 | 묶음(전/후) | 결과(성공/실패) | 소요시간(초) | 실패 원인 |
|---|---|---|---|---|
| 1~10 | 전 | ... | ... | ... |
| 11~20 | 후 | ... | ... | ... |

- **소요시간**: 성공 시 다른점 5개를 모두 찾는 데 걸린 시간(초). 실패 시 제한시간 전체(예: 180초)로 기록
- **실패 원인**: 시간 초과 시점까지 몇 번째 다른점을 못 찾았는지 기록 (예: "4/5 찾음, 5번째 위치차이 못 찾고 시간초과")

#### 점수/순위 계산 규칙 (T02-C21 및 홈 화면 랭킹과 공통 적용)
- 점수 기준: **소요시간(초)의 총합이 적을수록 순위가 높음** (실패한 판은 해당 단계 제한시간 전체를 소요시간으로 간주)
- 3단계 테스트에서는 이 소요시간 기록의 **범위(최소~최대)와 중앙값**을 변경 전/후 묶음별로 계산해 비교

#### 최종값 선택 기준 (T02-C21)
최종 위치차이 범위는 다음 중 하나 이상의 근거로 선택하고, 그 근거를 기록에 남긴다:
- 두 묶음의 소요시간 중앙값 비교 (더 적절한 평균 플레이 시간을 만드는 쪽)
- 두 묶음의 소요시간 범위(분산) 비교 (지나치게 쉽거나 어렵지 않은 쪽)
- 반복되는 실패 원인 패턴 비교 (예: 특정 범위에서 "마지막 1개를 못 찾아 실패"가 반복되면 해당 범위는 과도하게 어려운 것으로 판단)

#### 백로그 (이번 단계에서 보류)
- **개수 범위 튜닝**: 3단계 방법론과 동일하게 별도 라운드로 진행 예정
- **색깔차이 규칙 튜닝**: 3단계 방법론과 동일하게 별도 라운드로 진행 예정

### 이미지 기반 전환 (백로그에서 승격, 실제 이미지 제공됨) — 1차 버전 (폐기됨)

- ⚠️ 개정(최종, 1차 버전 자체가 폐기됨): 1단계에서 확정했던 "도형과 글자만 사용" 원칙은 **완전히 폐기**되었다. 1~5단계 전부 이미지 기반으로 전환했고, **도형 기반 렌더링 코드(`buildBoard`/`renderShape` 등)는 삭제**했다 — 더 이상 폴백으로도 남아있지 않다.
- (이력) 최초 구현은 `assets/`에 원본 사진 1장씩만 두고, 코드가 이미지 위 임의의 원형 영역을 골라 그 부분의 색감(hue-rotate/saturate)을 변형해서 다른점을 절차적으로 생성하는 방식이었다. 다른점 개수는 항상 5개로 고정, 반지름 범위는 단계별 `radiusRange`로 랜덤화했다.
- ⚠️⚠️ 개정(2026-09-16, 실제 "정답/오답 이미지 페어" 자산 제공됨에 따라 완전 대체): 위 절차적 생성 방식은 **폐기**되었다. 아래 "이미지 페어 기반 전환 (2차, 현재 버전)" 섹션이 유일한 현재 사양이다. 이 섹션은 과거 이력 기록 목적으로만 남긴다.

### 이미지 페어 기반 전환 (2차, 폐기됨)

- ⚠️⚠️ 개정(2026-09-16, 좌표 하드코딩 방식으로 재전환): 아래 "정답(다른점) 판정 방식" 절의 픽셀 단위 자동 탐지 방식은 **폐기**되었다. 이 절 전체(자동 탐지, 임계값/최소 면적/병합 로직, 알려진 한계 기록 포함)는 과거 이력 기록 목적으로만 남긴다. 유일한 현재 사양은 아래 "하드코딩 좌표 기반 전환 (3차, 현재 버전)" 섹션이다.

#### 원문 요청 요약
- 비교 그림의 소재를 procedural 생성이 아니라, `assets/` 아래 실제로 준비된 "기준 이미지 + 오답 이미지" 페어로 교체한다.
- 서로 관련 있는 이미지는 `assets/image숫자/` 폴더 안에 함께 보관된다. 폴더 내 `image숫자.png`는 기준(정답) 이미지, `image숫자-숫자.png`는 오답 이미지이며 오답은 폴더당 3종류 준비된다.

#### 확정 사양 (Q&A로 확정)

**1) 단계 수 vs 준비된 이미지 세트 수**
- 5단계 구조를 유지하기 위해 부족한 폴더를 image1로 돌려막지 않는다. **현재 assets에 준비된 `image숫자` 폴더 개수만큼만 단계를 구성한다.**
- 코드는 페이지 로드 시 `assets/image1/image1.png`부터 순서대로 존재 여부를 프로브(이미지 로드 성공/실패)하여 사용 가능한 이미지 세트 목록을 자동으로 만든다. 폴더 이름이나 개수를 코드에 하드코딩하지 않으므로, 나중에 `image2`, `image3`, ... 폴더가 추가되면 **코드 수정 없이** 다음 새 게임부터 자동으로 단계 수가 늘어난다.
- ⚠️ 개정(2026-09-16): 이후 `image2`~`image5` 세트가 모두 이미지+`diffs.js`까지 갖춰지면서 현재는 **총 5단계**로 진행된다. 앞으로 새 `image6`, `image7`, ... 폴더가 이미지+`diffs.js`를 모두 갖춘 채 추가되면 코드 수정 없이 다음 새 게임부터 단계 수가 그만큼 더 늘어난다.
- 각 단계의 제한시간은 기존 5단계 표(3:00/2:30/2:00/1:30/1:00)를 순서대로 그대로 사용한다(단계 수가 5보다 적으면 앞에서부터 그만큼만 사용). 오답 페널티(10초), 일시정지, 접근성 토글 등 이미지 교체와 무관한 기존 규칙은 전부 그대로 유지한다.

**2) 정답(다른점) 판정 방식**
- 좌표를 수동으로 미리 정하지 않는다. **게임(단계) 시작 시 코드가 기준 이미지와 그 단계에서 뽑힌 오답 이미지를 각각 캔버스에 그려 픽셀 단위로 비교**하고, 차이가 나는 픽셀들을 연결 요소(연결된 덩어리) 단위로 묶어 다른점 영역을 자동으로 찾아낸다.
- 각 연결 요소의 바운딩 박스 중심을 다른점의 좌표로, 바운딩 박스 크기를 기반으로 한 반지름을 클릭 판정 반지름으로 사용한다. 노이즈성 미세한 차이(안티에일리어싱 등)는 최소 면적 미만이면 다른점으로 세지 않는다.
- ⚠️ 이 변경으로 인해 "다른점은 항상 5개 고정" 규칙은 이미지 페어 방식에서는 더 이상 적용되지 않는다. **다른점 개수는 실제로 이미지에 존재하는 차이 개수(자동 탐지 결과)를 그대로 사용**하며, 화면 상태 표시("찾은 개수 / 전체 개수")도 이 자동 탐지된 총 개수를 기준으로 한다. (image1 세트의 오답 이미지들은 육안상 4~5개 내외의 차이를 담고 있음)
- 이전 버전의 "위치차이 범위(`radiusRange`)를 단계별로 랜덤화" 개념은 폐기된다. 반지름은 실제 탐지된 차이 영역의 크기에서 자동 계산되므로 별도의 난이도 파라미터가 아니다. 요구사항 3단계에서 진행했던 위치차이 범위 튜닝 기록은 폐기된 1차 절차적 생성 방식에 대한 역사적 기록으로만 남긴다.

**3) 화면 배치**
- 좌측 패널 = 기준 이미지(`image숫자.png`), 우측 패널 = 그 단계에서 랜덤으로 뽑힌 오답 이미지(`image숫자-N.png`) 고정. (이전처럼 좌우에 같은 이미지를 두고 오른쪽에만 다른점을 얹는 방식이 아니라, 실제로 서로 다른 두 장의 완성 이미지를 나란히 비교한다.)
- 다른점을 찾으면 좌/우 패널 모두에 얇은 원형 테두리(링)로 찾음 표시를 남긴다 (기존과 동일).

#### 오답 이미지 배정 규칙 (추가 요청으로 확정, 폴더 단위로 재정의)
- 오답 이미지는 폴더당 3종류이며, 각 단계 시작 시 그 단계에 배정된 이미지 세트(`image숫자` 폴더) 안에서 3종류 중 **랜덤으로 1개**를 골라 우측에 배치한다.
- 이미지 **세트(폴더) 배정**은 기존 셔플백 규칙을 그대로 유지한다: 바로 이전 단계에서 사용한 세트는 이번 단계에 다시 배정하지 않고, 이번 게임에서 아직 사용하지 않은 세트 중 무작위로 배정한다. 모든 세트를 이미 한 번씩 사용했다면 "바로 직전 단계 세트만 아니면" 되는 조건으로 완화한다. 사용 가능한 세트가 1개뿐이면 예외적으로 재사용을 허용한다(현재 상태).
- 이미지 세트 사용 이력은 "현재 판 값"이므로 저장하지 않으며, 새 게임을 시작할 때마다 초기화된다.

#### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C04/C05/C06: 다른점 개수가 이미지마다 달라질 수 있으므로, 상태 표시줄의 "찾은 개수 / 전체 개수"는 항상 그 단계에서 자동 탐지된 실제 총 개수를 기준으로 계산 및 표시한다.
- T02-C08/C09: 단계 전환/재시작 시 픽셀 비교로 탐지한 다른점 목록(hotspots)도 "현재 판 값"에 포함되어 매번 새로 계산되고 저장되지 않는다.

#### 알려진 한계 및 1차 임계값 선택 근거 (2026-09-16, 2차 보정)
- `image1` 세트의 오답 이미지 3종은 실제로는 서로 다른 원본 해상도로 제공되었다(`image1.png` 310×157, `image1-1.png` 1453×736, `image1-2.png` 310×157, `image1-3.png` 310×158). 해상도가 다른 두 이미지를 같은 캔버스 크기로 리샘플링하면 실제 다른점과 무관한 안티에일리어싱/리샘플링 노이즈가 다수 발생한다(임계값을 낮게 두면 노이즈가 20~30개 이상 잡힘).
- 픽셀 차이 임계값(채널합 120)과 연결 요소 최소 면적(이미지 면적의 0.15%, 최소 50px)으로 노이즈를 걸러내되, 하나의 실제 다른점이 여러 개의 인접한 연결 요소로 쪼개져 탐지되는 경우(예: 눈/입처럼 서로 떨어진 픽셀 그룹이 같은 캐릭터 안에서 별도 조각으로 잡히는 경우)를 위해 바운딩 박스가 가까운 조각끼리 하나로 합치는 병합 단계를 추가했다.
- ✅ 검증(2026-09-16): 실제 해상도가 기준 이미지와 같은 `image1-2.png` 조합에서 병합 전에는 5개 중 2개(입 모양 추가, 식은땀 표정)가 최소 면적 미만으로 누락되었으나, 병합+완화된 최소 면적 적용 후에는 육안으로 확인한 5개 다른점 모두가 정확한 위치에 탐지됨을 스크린샷으로 확인했다. 해상도가 크게 다른 `image1-1.png`/`image1-3.png` 조합에서는 여전히 매우 옅은 색조 변화 등 극히 미세한 차이 1개 정도가 노이즈와 구분되지 않아 누락될 수 있다(3개 탐지, 노이즈 재발은 없음).
- 이 값은 실제 3종 오답 이미지에 대한 반복 실행 비교로 정한 1차 보정값이며, 요구사항 3단계가 요구하는 정식 10회+10회 비교 방법론을 아직 거치지 않았다. 추후 이미지 세트가 늘어나거나 자산의 해상도가 통일되면, 임계값/최소 면적/병합 거리 값을 대상으로 정식 튜닝 라운드(T02-C18~C21)를 별도로 진행해야 한다.

### 하드코딩 좌표 기반 전환 (3차, 현재 버전 — 2026-09-16 확정)

#### 원문 요청 요약
- 픽셀 단위 자동 탐지 방식을 폐기하고, 다른점 좌표를 사람이 육안으로 확인해서 미리 정해두는 방식으로 되돌린다("고치지말고... 내가 틀린 부분 알려줄게" 요청으로 확정).

#### 확정 사양 (Q&A로 확정)
- **판정 방식**: 게임 실행 중 픽셀 비교를 전혀 하지 않는다. 이미지 세트 폴더(`assets/image숫자/`)마다 `diffs.js` 파일을 함께 두고, 그 안에 오답 이미지 파일명별로 다른점 좌표 배열(`{x, y, radius}`, 비율 0~1)을 사람이 직접 정의해둔다.
  - `diffs.js`는 `window.IMAGE_DIFFS.image숫자 = { "image숫자-N.png": [{x,y,radius}, ...], ... }` 형태의 전역 객체를 채우는 순수 데이터 파일이다.
  - 순수 JSON + `fetch()` 방식은 사용하지 않는다. 이 프로젝트는 서버 없이 `file://`로 시크릿 창에서 바로 열려야 하는데(T02-C01), `fetch`/`XHR`로 로컬 파일을 읽는 것은 브라우저가 `file://`에서 차단하기 때문이다. `<script src="...diffs.js">` 방식은 `<img src>`와 동일하게 `file://`에서도 정상 동작하므로 이 방식을 쓴다.
  - 좌표는 원본 이미지 픽셀이 아니라 이미지 가로/세로 대비 비율(0~1)로 저장한다. 유동형 레이아웃에서 이미지가 어떤 크기로 렌더링되든(리사이즈 등) 그대로 맞아떨어져야 하기 때문이다.
- **이미지 세트 탐지와의 연동**: `detectImageSets()`가 `image숫자.png` + 오답 이미지들의 존재를 프로브한 뒤, 같은 폴더의 `diffs.js`도 `<script>` 동적 로드로 함께 확인한다. `diffs.js`가 없거나, 값이 스키마(각 오답 파일명에 대해 `{x,y,radius}` 배열, x/y는 0~1, radius는 양수)를 만족하지 못하면 그 이미지 세트 전체를 사용 가능한 세트 목록에서 제외한다(게임이 깨지는 대신 조용히 건너뛴다).
  - 즉 이미지 세트를 새로 추가할 때는 이미지 파일뿐 아니라 **좌표가 채워진 `diffs.js`도 반드시 같은 폴더에 함께 추가**해야 그 세트가 실제로 게임에 등장한다.
- **다른점 개수**: 더 이상 자동 탐지 결과가 아니라 `diffs.js`에 사람이 정의해둔 배열의 길이 그대로다. 상태 표시줄("찾은 개수 / 전체 개수")은 이 값을 그대로 사용한다(기존 T02-C04/C05/C06 매핑과 동일하게 동작, 계산 방식만 자동 탐지 → 하드코딩 값 참조로 바뀜).
- **반지름/난이도 파라미터**: 픽셀 임계값·최소 면적·병합 거리 같은 자동 탐지 파라미터 개념 자체가 없어진다. 세트별 `diffs.js`에 적어둔 `radius` 값이 곧 클릭 판정 반지름이며, 값을 조정하려면 해당 좌표 항목을 직접 수정한다.
- **좌표 산출 방법(이력, 재현 가능하도록 기록)**: `image1` 세트의 5개 값은 다음 절차로 얻었다 — (1) 사람이 기준/오답 이미지를 육안으로 비교해 몇 번째 행·열 캐릭터가 다른지 지목, (2) 그 셀 영역만 잘라내어(인접 셀 오염 방지를 위해 셀 경계에서 8% 이내로만 여유를 둠) 채널합 임계값(30) 기준 픽셀 차이 바운딩 박스를 계산, (3) 바운딩 박스 중심을 좌표로, `max(가로,세로)*0.015`만큼 여유를 더한 값을 반지름으로 하되 클릭 편의를 위해 최소 반지름 0.035를 보장. 전체 이미지에 대해 한 번에 자동 탐지를 돌리지 않은 이유는 `image1-1.png`/`image1-3.png`가 압축 노이즈로 임계값을 낮추면 노이즈가 대량으로 잡히는 반면(2차 섹션의 "알려진 한계" 참고), 사람이 지목한 셀 안에서만 비교하면 그 노이즈 문제를 피할 수 있었기 때문이다.
  - 원본 데이터: `image1-1.png` → (0,1) 슬픈 표정, (1,1) X 얼굴, (1,5) 뒷모습(입 삭제), (2,4) 콘모자, (3,7) 파란색. `image1-2.png` → (0,4) X눈, (1,1) 메롱, (1,6) 색깔, (2,3) 눈물, (3,1) 눈썹과 눈. `image1-3.png` → (0,2) 뒷모습, (1,5) 화난 표정, (2,3) 눈물, (2,4) 색깔, (3,2) 눈. (행·열은 0부터 시작, 4행×8열 그리드 기준)
- **`image2` 세트 추가 (2026-09-16)**: `image2`는 균일한 격자가 아니라 배경 캐릭터들 사이에 큰 중앙 캐릭터가 있는 구도라 행·열 표기가 맞지 않는다. 대신 사람이 "중앙 공룡", "오른쪽 상단 공룡" 같은 위치/캐릭터 명칭으로 지목했고, 그 설명에 맞는 캐릭터/부위 영역만 잘라내 같은 방식(채널합 임계값 기준 바운딩 박스, `image1.png`/`image2-N.png`가 모두 1024×1024 동일 해상도라 리샘플링 노이즈가 없어 더 낮은 임계값 15~30을 사용)으로 좌표를 계산했다. 배경 전체 색이 바뀌는 것처럼 다른점 영역이 넓게 퍼진 경우(`image2-2.png`의 배경색 변화)는 바운딩 박스 전체 대신, 다른 캐릭터와 겹치지 않으면서 명확히 눈에 띄는 대표 지점 하나를 클릭 판정 좌표로 골랐다.
  - 원본 데이터: `image2-1.png` → 중앙 큰 공룡 표정, 좌중단 공룡 이빨 하나 없음, 좌중단 공룡 아래 작은 공룡 등장, 목 부근 가려진 공룡 눈 뜸, 그 아래 가려진 공룡 눈 뜸. `image2-2.png` → 중앙 공룡 머리털, 우상단 공룡 메롱, 우하단 공룡 입 모양, 중앙 위 공룡 배경색, 우측 가려진 공룡 눈 뜸. `image2-3.png` → 우상단 공룡 하트눈, 중앙 공룡 모자, 중앙 공룡 땀흘림, 좌측 가려진 작은 공룡 등장, 우하단 공룡 입 벌려 이 보임.
- **`image3` 세트 추가 (2026-09-16)**: `image3`(별 잡는 보트)은 기준 이미지(440×549)와 오답 이미지(약 920×1150)의 원본 해상도 차이가 커서, 기준 이미지를 오답 이미지 해상도로 업스케일링(LANCZOS)한 뒤 비교했다. 그래도 전체 이미지에 낮은 임계값을 걸면 구름/물결 선 등에서 리샘플링 테두리 노이즈가 다수 잡히므로, 사람이 지목한 부위 주변만 국소적으로 비교하거나(작은 변화), 전체 차이 이미지를 시각화해 "노이즈성 옅은 테두리"와 "진짜로 내용이 바뀐 선명한 덩어리"를 눈으로 구분해 좌표를 잡았다(넓게 퍼진 변화, 예: 보트 아래 별 유무).
  - `image3-2.png`의 "보트 아래 별" 항목은 사람이 "원래 없던 자리에 별이 생겼다"고 표현했지만, 실제로 픽셀 비교해보니 방향이 반대였다(기준 이미지에는 별이 있고 오답 이미지에서 그 별이 사라짐). 위치 자체는 일치했고, 사람이 위치만 맞으면 된다고 확인해줘서 그 좌표를 그대로 사용했다 — 다른점 판정은 "그 위치를 클릭하면 정답"이지 "무엇이 어떻게 바뀌었는지 방향"까지 맞힐 필요는 없기 때문에 문제 없음.
  - 원본 데이터: `image3-1.png` → 오른쪽 상단 큰 달, 공룡 표정, 채 안 하트 모양, 채(그물) 방향, 강아지가 문 공. `image3-2.png` → 오른쪽 하단 다른 공룡 머리, 보트 앞 모터, 공룡 위쪽 별똥별, 공룡 발, 보트 아래 별 유무 변화. `image3-3.png` → 왼쪽 위 다이빙하는 물고기, 공룡 눈, 채 안 돌맹이와 별 개수, 보트 안 밧줄, 강아지 얼굴과 발.
- **`image4` 세트 추가 (2026-09-16)**: `image4`(동물 편지)는 `image4-1.png`만 기준 이미지(549×553)보다 해상도가 커서(1024×1031) 업스케일 비교, `image4-2.png`/`image4-3.png`는 기준과 해상도가 같아 리샘플링 노이즈 없이 바로 비교했다. 편지 손글씨 텍스트 자체는 업스케일된 `image4-1.png` 비교에서 전체적으로 옅은 테두리 노이즈가 나는데(문자가 작고 촘촘해서), 실제 의미 있는 글자 변경(`image4-2`/`image4-3`의 오탈자·문장 차이)은 텍스트가 굵고 뚜렷하게 다른 위치에서만 확인해 노이즈와 구분했다.
  - `image4-1.png`에서 자동/육안 비교 중 사람이 지목한 5개 목록에 없던 "용 머리 볏 색깔(빨강→노랑)+뿔 생김" 변화를 추가로 발견해 확인을 구했다. 사람이 이것과 원래 5번 항목("4번 공룡 위의 임팩트 색깔")을 같은 다른점으로 봐 달라고 확정해줬다 — 즉 용 머리 볏 변화 = 5번 항목이며, 별도의 6번째 다른점이 아니다. 마찬가지로 뾰족등 공룡 입가의 작은 파란 눈물자국이 사라지는 변화도 "그 공룡 자체(4번 항목, 색깔 변화)"에 포함되는 것으로 정리했다. 결과적으로 `image4-1.png`도 다른 세트와 동일하게 정확히 5개의 다른점으로 마무리된다.
  - 원본 데이터: `image4-1.png` → 오른쪽 익룡 색깔, "토!" 말풍선, 보라 공룡 눈, 뾰족등 공룡 색깔(+눈물자국), 용 머리 볏 색깔+뿔. `image4-2.png` → 오른쪽 익룡 눈 2개, 뾰족등 공룡 엉덩이 밴드, 편지 하단 발자국 모양, "제발요."의 "요.", "ㅠㅠㅜㅠ" 마지막 ㅠ 없음. `image4-3.png` → 익룡 머리 위 바람 임팩트 없음, 익룡 다리 3개, 왼쪽 하단 몰래 보는 초록 공룡, "마니 아푸다"의 "푸", "우리 구하러 와조 제발" 부분(원문 "와조세요").
- **`image5` 세트 추가 (2026-09-16)**: `image5`(공룡 무리, 사람 모양 몸통에 목만 다양하게 변형된 캐릭터들)는 기준 이미지(`image5.png`, 442×552)가 오답 이미지들(`image5-1.png` 3360×4180, `image5-2.png` 4780×5952, `image5-3.png` 3492×4348)보다 훨씬 저해상도라 리샘플링 노이즈 문제가 있었지만, 다른점 좌표는 이번에도 자동 탐지가 아니라 전부 사람이 육안으로 지목한 위치를 기준으로 잡았다(격자 좌표를 그려 확대 비교 후 중심/반지름 산출, 최소 반지름 0.035 보장).
  - 우측 상단에 목이 긴 캐릭터(사람 몸통에 목만 길게 늘어난 디자인)는 세 오답 이미지와 기준 이미지 모두에 동일한 위치·포즈로 이미 등장하고 있어 **다른점이 아니라 원래부터 있던 고정 배경 요소**임을 확인했다(한때 이 캐릭터를 실제 브라키오사우루스 그림으로 교체하는 이미지 편집을 시도했으나, 사용자 요청으로 취소하고 원본 그대로 되돌렸다 — 아래 "폐기된 시도" 참고).
  - 원본 데이터: `image5-1.png` → 왼쪽 위 공룡의 프로펠러 모자, 우측 상단 안드로이드 로고, 왼쪽 최하단 공룡 눈 한쪽 없음, 중앙(오른쪽) 공룡의 가방, 그 공룡의 웃는 표정. `image5-2.png` → 왼쪽 최상단 공룡 선글라스, 왼쪽 하단 공룡 입에 문 잎사귀(사람이 "오른쪽"이라 했으나 실제로는 왼쪽 최하단이었음 — 위치 자체는 사람이 확인해줌), 최하단 중앙 공룡 모자, 오른쪽 무리에 원래 없던 작은 공룡이 새로 추가됨, 오른쪽 바닥 타일 위 개미. `image5-3.png` → 왼쪽 최상단 공룡 이마의 포스티잇, 그 아래 줄 공룡이 든 사탕(롤리팝), 그 오른쪽의 아이스크림 먹는 작은 공룡 삽화, 오른쪽 캐릭터 아래 바닥 그림자 속에 숨은 눈, 최하단 중앙 공룡 왼쪽 아래 떨어진 쓰레기 조각.
  - **폐기된 시도(2026-09-16)**: 사용자가 새로 그린 브라키오사우루스 그림(배경 없음, 옆모습, 오른쪽을 바라봄)을 받아 세 이미지의 기존 "목이 긴" 캐릭터를 이 그림으로 교체하는 합성을 시도했었다. 배경을 색상 기준으로 투명화하고, 기존 캐릭터 자리를 주변 바닥 타일로 복원한 뒤 새 그림을 같은 위치에 비율에 맞게 다시 그려 넣었으나, 사용자가 "무슨 말인지 모르겠다"며 작업 취소를 요청해 **원본 이미지로 완전히 되돌렸다**. 이미지 자산 자체를 사람이 직접 준비하는 기존 원칙(요구사항 3단계 "이미지 페어 기반 전환" 섹션 참고)을 유지하며, 이후 이 캐릭터를 실제로 교체하고 싶다면 별도의 명확한 요청과 확인 절차를 거친다.
  - **폐기된 시도(2026-09-17, 이미지 세로 높이 통일)**: 세트마다 원본 PNG 세로 높이가 제각각이라 20개 파일 전부를 그 시점 최소 높이(157px)로 리샘플링해 통일을 시도했으나, 실제 적용해보니 화면에서 이미지가 심하게 깨져 보여(저해상도 확대로 인한 화질 저하) 사용자가 원본으로 되돌려달라고 요청했다. **원본 PNG 해상도는 그대로 유지**하며(세트 간 원본 해상도 편차 존재), 대신 아래 "보드 박스 비율 고정" 방식으로 대체 확정했다.
- **보드 박스 비율 고정 (2026-09-17 확정, challenges.md 7번 최종 해결)**: 이미지 파일 자체를 리사이즈하는 대신, 좌/우 보드 박스(`.board`)의 **틀(비율) 자체**를 `image4`(549×553, 거의 정사각형, 비율 549/553)에 고정했다. 사용자가 `image4`/`image4-3` 조합이 나왔을 때 패널 하단에 생기는 여백 크기가 마음에 든다고 확인해줬고, 그 여백은 "박스가 남은 세로 공간을 끝까지 채우지 않고 image4 비율에서 멈추기 때문"임을 확인한 뒤 이 비율을 모든 단계에 동일하게 고정하기로 확정했다.
  - **적용 방법**: `.board`를 `flex: 1 1 auto`(남은 공간을 다 채움)에서 `flex: 0 1 auto; aspect-ratio: 549 / 553;`로 변경했다. 이제 박스 높이는 패널 너비를 기준으로 이 비율만큼만 계산되고, 남는 세로 공간은 `.board-panel`의 기본 정렬(`justify-content: flex-start`)에 의해 항상 박스 **아래쪽**에 남는다 — 이것이 사용자가 마음에 든다고 한 "하단 흰 공백"이다. 어떤 이미지 세트가 나오든 박스 크기 자체는 항상 동일하다(1366×768 기준 약 590px 높이, 1920×1080 기준 약 585px 높이로 검증).
  - **비율이 다른 이미지 처리**: `image1`(가로로 긴 비율 약 1.97)이나 `image3`/`image5`(세로로 긴 비율 약 0.8)처럼 549/553과 비율이 다른 세트는 박스를 채우지 못하고 기존과 동일하게 `object-fit: contain`으로 박스 안에서 레터박스(빈 여백) 처리된다 — 잘리지 않는다.
  - **검증**: `/browse`로 1366×768, 1920×1080 두 해상도 모두에서 여러 단계(랜덤 세트)를 반복 로드해 `.board`의 실제 렌더링 높이가 세트와 무관하게 항상 동일함을 확인했고, 가로/세로 스크롤(overflow)도 발생하지 않았다(요구사항 2단계 유동형 레이아웃 원칙 유지).

#### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C01: `diffs.js`를 `<script src>`로 로드하므로 서버 없이 `file://`로 열어도 정상 동작한다 (JSON+fetch였다면 이 기준을 깼을 것).
- T02-C04/C05/C06: "찾은 개수 / 전체 개수"는 `diffs.js`에 정의된 배열 길이를 그대로 총 개수로 사용한다.
- T02-C08/C09: 하드코딩된 좌표 자체는 "보존 기록"이 아니라 정적 자산이므로 저장 대상이 아니다. 매 단계 시작 시 이 값을 읽어와 "현재 판 값"(`state.boardData.hotspots`)에 채워 넣을 뿐, 판마다 새로 계산되지 않고 항상 동일하다(자동 탐지 시절과 달리 매 판 값이 달라지지 않음 — 의도된 변화).

### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C18 / T02-C19: 변경 전/후 각각 정확히 10회씩 플레이 기록을 20회 플레이표에 남김
- T02-C20: 한 라운드에서는 위치차이 범위 1개 파라미터만 변경, 개수/색깔규칙/제한시간은 두 묶음에서 완전히 동일하게 고정
- T02-C21: 최종 선택 이유를 소요시간 중앙값/범위 비교 또는 반복 실패 원인과 명시적으로 연결하여 기록

### 막힘 방지 체크리스트
- 여러 값을 동시에 바꾼 것을 발견하면:
  1. 변경 전 설정으로 복원
  2. 이번 라운드에서 바꿀 값 1개만 선택
  3. 나머지 파라미터를 고정한 뒤 재실행

---

## 요구사항 4단계

### 원문 (하는 일 / 완료 목표 / 통과 기준)

- 하는 일: 현재 판 값과 보존 기록을 나누고 빈 값·손상값을 안전하게 처리한다.
- 완료 목표: 새 판 초기화·재접속 보존·손상 복구를 함께 확인한다.
- 첫 행동: 초기화할 값과 보존할 값을 목록으로 나눈다. 정상·빈 값·손상 값에서 새 판과 재접속을 시험한다.
- 통과 기준
  - T02-C22 새 게임에서 현재 판 값이 초기화된다.
  - T02-C23 보존하기로 선언한 기록은 재접속 뒤 유지된다.
  - T02-C24 빈 저장값에서도 게임이 중단되지 않고 선언한 기본값으로 시작한다.
  - T02-C25 손상된 저장값에서도 게임이 중단되지 않고 선언한 기본값으로 시작한다.
- 막히는 지점과 확인 순서: 손상 값에서 게임이 멈추는 경우 → 형식 검사 → 기본값 선언 → 상태·화면·타이머 함께 초기화
- 남길 것: 초기화·보존·손상 복구 결과

### 확정 사양 (Q&A로 확정)

#### 값 분류: 초기화 대상 vs 보존 대상

**초기화 대상 ("현재 판 값" — 메모리에만 존재, 저장하지 않음)**
- 현재 단계 번호(1~5)
- 남은 시간(타이머)
- 찾은 개수 / 이번 판에 배치된 도형(개수·위치·색깔의 무작위 결과)
- 일시정지 여부
- 오답 피드백 등 일시적 UI 상태
- 입력한 플레이어 이름(해당 세션 한정 — 새로고침하면 다시 입력)

**보존 대상 ("보존 기록" — `localStorage`에 저장, 재접속 후에도 유지)**
- 순위(랭킹) 기록: 이름별 **최고(최소) 총 소요시간** 리스트 (기존 요구사항 1단계의 순위 페이지 데이터와 동일)

#### 초기화 트리거 (T02-C22)
- "새 게임"(홈 화면에서 "게임 시작" 클릭) 시 현재 판 값 전체 초기화 후 1단계부터 시작
- **페이지 새로고침/재접속은 항상 홈 화면으로 돌아간다.** 진행 중이던 단계·타이머 등 현재 판 값은 보존하지 않으며, 이름도 다시 입력해야 한다 (요구사항 1단계의 "재시작 시 홈으로 리셋" 원칙과 동일하게, 새로고침도 사실상 새 게임과 동일하게 취급)

#### 재접속 보존 확인 (T02-C23)
- 재접속(새로고침, 탭을 닫았다가 다시 열기) 뒤에도 `localStorage`의 순위 기록은 그대로 유지되어야 함
- 순위 기록은 오직 "5단계 클리어로 최종 기록이 저장되는 시점"에만 갱신되고, 그 외의 경우(중도 실패, 새로고침 등)에는 절대 변경되지 않음

#### 저장 스키마 및 손상 판정
- `localStorage` 키: `spot-the-diff:rankings` (예시, 구현 시 확정)
- 저장 형식: `{ version: 1, records: [{ name: string, totalTimeSeconds: number, achievedAt: string(ISO) }, ...] }`
- 유효성 검사 규칙: JSON 파싱 성공 여부, 최상위가 객체인지, `version`이 기대값인지, `records`가 배열인지, 각 원소가 `{name: non-empty string, totalTimeSeconds: number >= 0}` 형태인지 확인

#### 빈 값 처리 (T02-C24)
- 해당 키가 아예 없는 경우(최초 방문) → 기본값 `{ version: 1, records: [] }`로 간주하고 정상적으로 순위 페이지에 "기록 없음" 표시, 게임 진행에는 영향 없음

#### 손상 값 처리 (T02-C25)
- JSON 파싱 실패, 예상 타입과 다름, 스키마 불일치 등 유효성 검사를 하나라도 통과하지 못하면 **저장값 전체를 신뢰하지 않고 빈 순위(`{ version: 1, records: [] }`)로 리셋**
- 리셋 즉시 정상 기본값을 `localStorage`에 다시 써서 다음 접속부터는 정상 상태로 유지
- 이 과정에서 예외가 상위로 전파되어 게임 초기화·렌더링을 막아서는 안 됨 (반드시 try/catch로 감싸고, 실패 시 기본값 사용) → 콘솔 빨간 오류(T02-C17)로 이어지지 않도록 처리

### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C22: "게임 시작" 클릭 및 새로고침/재접속 시 현재 판 상태를 항상 초기값으로 재생성
- T02-C23: 순위 기록만 `localStorage`에 저장하고, 오직 5단계 클리어 시점에만 갱신 → 그 외 상황에서 손대지 않음
- T02-C24: 저장 키 부재 시 기본값(`{version:1, records:[]}`)으로 폴백, 정상 진행
- T02-C25: try/catch + 스키마 검증으로 손상 데이터를 감지하면 기본값으로 리셋 후 즉시 재저장, 예외 전파 금지

### 막힘 방지 체크리스트
- 손상 값에서 게임이 멈추는 경우 확인 순서:
  1. 저장값 형식 검사(JSON 파싱, 스키마 검증)가 실제로 실행되는지 확인
  2. 검사 실패 시 기본값 선언이 빠짐없이 이루어지는지 확인
  3. 상태·화면·타이머가 함께(부분적으로 남지 않고) 초기화되는지 확인

---

## 요구사항 5단계

### 원문 (하는 일 / 완료 목표 / 통과 기준)

- 하는 일: 중요한 사건의 효과와 음소거 또는 움직임 감소를 함께 만든다.
- 완료 목표: 효과가 정한 사건에서만 실행되고 즉시 줄거나 멈추게 한다.
- 첫 행동: 성공·실패·충돌 중 효과를 줄 사건 하나를 고른다. 음소거 또는 움직임 감소를 켜고 같은 사건을 다시 시험한다.
- 통과 기준
  - T02-C26 선택한 효과는 선언한 사건에서만 실행된다.
  - T02-C27 음소거 또는 움직임 줄이기를 켜면 해당 효과가 즉시 줄거나 멈춘다.
  - T02-C28 공개 화면과 제출물의 개인정보가 0건이다.
  - T02-C29 공개 화면·배포 파일·제출물의 비밀값 원문이 0건이다.
  - T02-C30 짧은 확인 방법에 ① 어디로 가나요, ② 3단계 이내 무엇을 하나요, ③ 무엇이 보이면 통과인가요, ④ 안 될 때 무엇이 보이나요가 구분되어 있다.
  - T02-C31 제출문에 ① AI에게 맡긴 일, ② 학생이 직접 판단한 일, ③ AI 제안을 따르지 않은 일(없으면 없었던 이유)이 구분되어 있다.
- 막히는 지점과 확인 순서: 효과가 계속 반복되는 경우 → 사건 조건 1회성 확인 → 효과 종료 확인 → 공통 설정값 확인
- 남길 것: 효과 사건·끄기 선택·비밀값 점검

### 확정 사양 (Q&A로 확정)

#### 효과 대상 이벤트 (T02-C26)
- 최종적으로는 **성공(단계 클리어) / 실패(시간초과) / 충돌(오답 클릭)** 세 이벤트 모두에 효과를 붙일 예정
- **1차 구현·검증 대상: 단계 성공(5개 다 찾음)** — 이 이벤트에 먼저 소리+애니메이션 효과를 붙이고 통과 기준을 검증한다
- ⚠️ 개정(2026-09-17, 오답 클릭 효과 백로그에서 승격): 오답(충돌) 클릭 효과를 구현했다. 실패(시간초과) 효과는 아직 백로그로 남아 있다.
  - 기존에 화면 하단에 띄우던 "오답입니다 (-10초)" 텍스트 문구는 **제거**하고, 대신 클릭한 바로 그 위치에 빨간 X 표시 + 보드(좌우 그림, `#boards`) 영역만 짧게 흔들리는 효과로 완전히 대체했다(사용자가 텍스트 대체를 확정).
  - X 표시는 애니메이션이 아니라 정적 표시라서 움직임 감소 토글과 무관하게 항상 보여준다. 흔들림만 "움직임"이므로 움직임 감소가 켜져 있으면 아예 클래스를 붙이지 않아 실행되지 않는다(사용자가 "보드 영역만 흔들림, 움직임 감소 시 꺼짐"으로 확정 — 페이지 전체를 흔드는 방식은 채택하지 않음).
  - X 표시는 클릭된 캔버스 위에 500ms만 그려졌다가 `redrawBoards()`로 지워진다(찾은 표시 등 기존 그림은 그대로 유지). 흔들림은 0.4s CSS 애니메이션이며 `animationend`에서 클래스를 제거한다(요소당 리스너 1개 유지, 빠르게 연속 오답 클릭 시에는 클래스 제거→강제 리플로우→재부착으로 애니메이션을 재시작한다).
- ⚠️ 개정(2026-09-17, 정답 클릭 효과 추가): 다른점을 정확히 찾았을 때, 기존의 초록색 원(찾음 표시) 자리에 짧은 폭죽 파티클 효과를 추가로 터뜨린다.
  - **위치**: 클릭해서 찾은 그 다른점의 좌표(`diffs.js`의 hotspot `x`,`y`, 즉 초록 원과 정확히 같은 지점)에서 터진다.
  - **범위**: 초록 원은 기존처럼 좌/우 두 판 모두에 그려지지만, 폭죽은 **실제로 클릭한 쪽 판에만** 터진다(사용자가 확정 — 반대쪽 판은 원만 표시되고 폭죽은 없음).
  - **구현**: 캔버스에 그리지 않고, 클릭된 캔버스의 부모 `.board`(`position:relative; overflow:hidden`) 안에 `span.firework-particle` 여러 개 + 충격파 링(`span.firework-ring`) 1개를 절대 위치로 생성해, 파티클은 각자 다른 각도/거리로 퍼지며 사라지게, 링은 중심에서 빠르게 커지며 사라지게(`opacity`+`transform` 애니메이션) 한 뒤, 각각 `animationend`에서 즉시 DOM에서 제거한다(요소가 그때그때 새로 생성·삭제되므로 "요소당 리스너 1개" 원칙은 각 인스턴스 기준으로 지켜진다). `.board`가 `overflow:hidden`이라 판 가장자리 근처에서 터지면 일부가 잘려 보일 수 있는데, 판 밖으로 새어나가지 않게 하려는 의도된 동작이다.
  - ⚠️ 개정(2026-09-17, 임팩트 보강 1차): 처음 버전은 파티클이 작고(7px) 퍼지는 반경도 좁아서(26~48px) 커스텀 마우스 커서 아이콘에 가려 거의 안 보인다는 피드백을 받았다. 파티클 개수(12→18), 크기(8~14px), 퍼지는 반경(55~120px)을 늘리고, 충격파 링과 파티클 발광(`box-shadow`)을 추가했다.
  - ⚠️ 개정(2026-09-17, 임팩트 보강 2차): "그냥 크게 폭죽 터뜨려줘" 요청으로 한 번 더 키웠다. 현재 값: 파티클 28개, 크기 10~19px, 퍼지는 반경 90~190px, 지속시간 0.85초(파티클)/0.65초(링), 충격파 링은 최대 24px×11배(약 264px 지름)까지 커진다.
  - **움직임 감소와의 관계**: 폭죽(파티클+충격파 링 모두)은 움직이며 사라지는 애니메이션이므로 움직임 감소 토글이 켜져 있으면 둘 다 아예 생성하지 않는다(사용자가 확정). 초록 원은 정적 표시라 움직임 감소와 무관하게 항상 그려진다.
- 효과는 선언한 사건이 실제로 발생한 그 순간에만, **정확히 1회** 실행되어야 한다 (반복 재생·잔여 트리거 금지)

#### 효과 종류
- **소리**: 짧은 비프/차임 사운드 (도형/글자 기반 원칙과 무관하게 오디오는 허용)
- **애니메이션**: 도형이 짧게 번쩍이거나 확대/축소되는 등의 짧은 CSS/JS 애니메이션 (이미지 아님, 도형 기반 유지). 오답 클릭의 보드 흔들림(`translateX` 왕복), 정답 클릭의 폭죽 파티클도 이 범주.
- **정적 표시**: 오답 클릭 위치의 빨간 X 표시, 정답 위치의 초록색 원처럼 움직이지 않는 시각 피드백은 애니메이션이 아니므로 움직임 감소 토글의 대상이 아니다.
- 애니메이션형 효과(성공 시 반짝임, 오답 시 흔들림, 정답 시 폭죽)는 움직임 감소 토글로 동시에 꺼지고, 소리는 음소거 토글로만 제어되어 서로의 재생에 영향을 주지 않는다.

#### 접근성 토글 (T02-C27)
- **음소거 토글**과 **움직임 감소 토글**을 각각 별도로 제공한다.
  - ⚠️ 개정(2026-09-17): 화면마다 따로 두던 방식을 폐기하고, **뷰포트 왼쪽 상단에 고정된 전역 컨트롤 하나**로 통합했다. 홈/규칙 확인/게임/순위 어느 화면이든 항상 같은 위치에서 제어 가능하다. 자세한 내용은 요구사항 1단계의 "접근성 토글 상시 노출 범위" 절 참고.
  - ⚠️ 개정(추가 요청, 표시 방식 변경): 체크박스 대신 **아이콘 버튼**(`<button aria-pressed>`)으로 표시한다. 버튼 자체(pill)는 켜지면 배경이 진하게 반전된다(`.icon-toggle[aria-pressed="true"]`). 요소당 이벤트 리스너는 1개(click)만 등록한다.
  - ⚠️ 개정(2026-09-17, 아이콘 모양 재작업): CSS 가짜요소(`::before`/`::after`) 대신 인라인 `<svg>`(`.icon-graphic`, `viewBox 0 0 24 24`)로 바꿨다. 여러 후보를 만들어 비교한 뒤 다음 두 개를 확정했다:
    - **음소거**: 스피커 + 소리 물결 2겹(`.icon-wave`). 음소거를 켜도 물결은 지우지 않고 그대로 두며, 대각선 슬래시(`.icon-slash`, `x1=3 y1=3 x2=21 y2=21`)만 추가로 겹쳐 그린다(`aria-pressed="true"`일 때 `opacity: 1`). 처음에는 음소거 시 물결을 통째로 숨겼었는데, "음파 표시까지 없애는 건 되살려 달라"는 요청으로 물결은 항상 보이게 되돌렸다.
    - **움직임 감소**: 점 + 퍼지는 물결(리플) 2겹(`.icon-ring`, `.icon-ring-outer`). 슬래시를 쓰지 않고, 움직임 감소를 켜면 바깥쪽 리플(`.icon-ring-outer`)만 `opacity: 0`으로 꺼서 "움직임이 잦아든" 느낌을 낸다.
    - 두 아이콘 다 비교용 시안 단계에서 대각선 슬래시가 아이콘의 다른 요소(막대·화살촉 등)와 겹쳐 "지워진 것처럼" 보이는 문제가 있었는데, 실제로 채택한 두 디자인(물결/리플)은 그 문제가 없는 조합으로 확인 후 반영했다.
- 설정값은 즉시 적용: 토글을 켜는 시점에 이미 재생 중인 효과가 있다면 그 자리에서 즉시 중단(소리는 정지, 애니메이션은 취소)하고, 이후 발생하는 같은 이벤트에서도 해당 효과를 재생하지 않음
- 설정은 세션 내 유지되면 충분 (요구사항 4단계의 "보존 대상"에는 포함하지 않음 — 필요 시 이후 별도 지시로 `localStorage` 보존 여부 결정)

#### 개인정보/비밀값 점검 (T02-C28, T02-C29)
- 플레이어 이름은 사용자가 임의로 입력하는 표시용 별명으로 취급하며, 실명·이메일·전화번호 등 실제 개인정보 입력을 유도하는 문구를 어디에도 넣지 않는다
- 콘솔 로그, 화면, 저장 데이터 어디에도 개인정보(주민번호, 연락처, 이메일 등)를 남기지 않는다
- 프로젝트는 백엔드/외부 API 연동이 없으므로 API 키·토큰 등 비밀값이 원천적으로 필요 없다. 소스코드·배포 파일 어디에도 비밀값(하드코딩된 키/토큰/비밀번호)을 절대 넣지 않는다
- 배포/제출 전 체크: 소스 전체에서 `key`, `token`, `secret`, `password` 등 문자열을 검색해 비밀값이 하드코딩되지 않았는지 확인

#### 제출용 문서 (T02-C30, T02-C31)
- 확인됨: 이 두 항목은 **제출 시점에 별도로 작성하는 제출 문서**이며, 게임 자체의 기능 요구사항에는 포함하지 않는다
- 이 저장소의 `GAME_RULES.md`/`CLAUDE.md`에는 스펙으로 반영하지 않으며, 제출 단계에서 별도 요청이 오면 그때 작성한다

#### 클로드 아이콘 미니 이벤트 (⚠️ 추가 요청으로 신설, 2026-09-17)
- **범위 메모**: 이 항목은 요구사항 5단계가 이미 확정된 뒤 추가된 장식/보너스 성격의 신규 기능이며, 요구사항 3단계의 "난이도 튜닝 원칙"(10회+10회 비교, 파라미터 1개만 변경)이 적용되는 "난이도 관련 값"으로 취급하지 않는다. 아래 구체 수치(간격/지속시간/개수 등)는 감으로 잡은 초기값이며, 이후 "너무 잦다/드물다" 같은 피드백이 오면 그때 조정한다.
- **등장 시점/빈도** (Q&A로 확정): 게임 화면에서 실제로 플레이가 진행되는 동안(일시정지 중이거나 단계 성공/실패 오버레이가 떠 있는 동안은 제외) 반복 등장한다.
  - ⚠️ 개정(2026-09-17, "5초 단위로" 요청): 스폰 간격을 무작위(8~18초)에서 **고정 5초**로 바꿨다. 이때 한 번에 1~3개씩 몰아 나오던 것도, 5초마다 매번 여러 개가 겹치면 너무 잦고 어지러워지므로 일시적으로 **5초마다 정확히 1개씩**만 나오도록 단순화했었다.
  - ⚠️ 개정(2026-09-17, "1개~4개까지 등장하게" 요청): 위에서 1개로 단순화했던 것을 다시 되돌려, **스폰 파도(5초)마다 1~4개가 무작위 개수로 등장**하도록 바꿨다(기존 1~3개 범위에서 4개까지 확장).
  - 화면에 동시에 떠 있는 아이콘 상한(구현상 안전장치)도 3개에서 12개로 올렸다. 이동 속도가 1/3로 느려지고 대각선 이동까지 생기면서 아이콘 한 개가 화면에 머무는 시간이 5초보다 길어질 수 있어(먼 대각선일수록 오래 걸림), 이전 스폰 파도의 아이콘이 남아있는 채로 다음 파도(최대 4개)가 겹칠 수 있기 때문이다. 상한을 낮게 두면 정상적인 1~4개 스폰이 조용히 줄어드는 문제가 생겨, 평소에는 절대 걸리지 않을 넉넉한 값(12)으로 잡았다.
  - ⚠️ 개정(2026-09-17, "1단계 5초, 2단계 4.5초.. 5단계까지 0.5초씩 줄여줘" 요청): 모든 단계에서 똑같이 5초였던 스폰 간격을, `STAGE_TIME_LIMITS`(단계별 제한시간)와 동일한 패턴으로 **단계마다 0.5초씩 짧아지게** 바꿨다. 값(1차): 1단계 5.0초 / 2단계 4.5초 / 3단계 4.0초 / 4단계 3.5초 / 5단계 3.0초.
  - ⚠️ 개정(2026-09-17, "5,4,3,2,1초로 설정해줘" 요청): 위 0.5초 간격 값을 다시 **1초 간격**으로 바꿨다. 값: 1단계 5초 / 2단계 4초 / 3단계 3초 / 4단계 2초 / 5단계 1초.
  - ⚠️ 개정(2026-09-17, "스폰 간격 전부 1/3로 줄여줘" 요청, 최종): 위 5개 값을 각각 그대로 1/3로 줄였다. 최종값(반올림, ms): **1단계 1667ms / 2단계 1333ms / 3단계 1000ms / 4단계 667ms / 5단계 333ms**. 단계 간 비율(5:4:3:2:1)은 그대로 유지된다. 배열 길이(5단계)를 넘는 단계가 있으면 `STAGE_TIME_LIMITS`처럼 마지막 값(333ms)을 그대로 쓴다.
  - ⚠️ 주의: 5단계는 스폰 간격(333ms)이 매우 짧아지는 반면, 아이콘 한 개의 이동 시간은 여전히 초 단위(속도 0.34px/ms 기준, 화면을 가로지르는 데 보통 수 초)라 새 스폰이 이전 스폰보다 훨씬 빨리 반복된다. 그 결과 화면에 동시에 떠 있는 아이콘 수가 빠르게 늘어나 "동시 표시 상한(12개)"에 자주 걸릴 수 있다(상한에 걸리면 그 파도의 스폰 개수가 줄어들거나 건너뛴다). 요청받은 값 그대로 반영했으며, 상한 자체를 같이 조정해달라는 별도 지시는 없어 그대로 뒀다.
- **이동 방식**: 1차 구현은 화면 폭을 가로지르는 좌↔우 수평 이동만 있었고(왕복 방향 무작위, 세로 위치 무작위, 지속시간 1.2~1.8초 무작위), easing은 ease-in-out(가속/감속 있음)이었다.
  - ⚠️ 개정(2026-09-17, "속도 1/3, 일정한 속도로" + "대각선 아무 방향" 요청): 두 가지를 동시에 바꿨다.
    1. **속도**: easing을 linear(등속)로 바꾸고, "이동 거리(px) ÷ 고정 속도(px/ms)"로 매번 duration을 계산하는 방식으로 바꿨다. 대각선처럼 이동 거리가 길어져도 속도 자체는 항상 동일하다(예전처럼 "이동 거리와 무관하게 duration만 무작위"였다면 대각선이 더 빨라 보였을 것). 속도값은 이전 버전의 평균 체감 속도(가로 폭 1366px + 양쪽 버퍼 180px ≈ 1546px를 평균 1500ms에 통과 ≈ 1.03px/ms)의 **1/3인 약 0.34px/ms**로 낮췄다.
    2. **방향**: 좌↔우 수평 이동만 가능하던 것을, 뷰포트의 상/하/좌/우 네 변 중 무작위로 고른 시작 변의 화면 밖 지점에서, 시작 변과 다른 변의 화면 밖 지점으로 이동하도록 바꿨다(같은 변으로 다시 빠지지 않게 해 너무 짧은 이동이 나오는 것을 방지). 그 결과 가로/세로/대각선이 모두 나올 수 있다.
  - 구현상 두 지점이 매번 달라 고정된 CSS `@keyframes` 두 개로는 표현할 수 없어, Web Animations API(`element.animate()`)로 시작/끝 좌표(px)를 그때그때 계산해 넣는 방식으로 바꿨다(이동 관련 코드에만 해당, 다른 이펙트는 기존 CSS 애니메이션 방식 그대로 유지).
  - ⚠️ 개정(2026-09-17, "최대한 중앙을 지나가도록" 요청): 도착점을 시작점과 무관하게 또 다른 변에서 완전히 무작위로 뽑던 방식은 폐기했다. 그러면 두 화면 구석을 잇는 경로처럼 중앙에서 한참 벗어나는 경우가 잦았기 때문이다. 대신 "시작점 → 화면 정중앙 부근(가로/세로 각각 ±12.5% 무작위 흔들림) → 그 방향을 그대로 이어서 화면 밖으로 나가는 지점"으로 도착점을 계산한다. 시작 변·시작 위치는 여전히 완전히 무작위라 "아무 방향에서" 오는 것은 그대로 유지되고, 경로 자체가 매번 중앙 부근을 지나가게만 바뀐 것이다(요청한 "아무 방향에서 아무 방향으로"와 "최대한 중앙을 지나가도록"을 동시에 만족).
- **모양** (Q&A로 확정: "클로드니까 클로드를 직접 만들어라"): 1차 구현은 자체 제작한 추상 선버스트 SVG였다.
  - ⚠️ 개정(2026-09-17, 이미지 파일로 교체 1차): "클로드 마스코트 이미지를 가져와달라"는 추가 요청에 따라 웹을 검색했으나, Anthropic이 무료로 배포하는 마스코트 이미지 파일은 없었다(유료 일러스트 팩과 저작권이 불명확한 팬아트만 검색됨). 검색 과정에서 확인한 사실은 Claude Code 터미널에 등장하는 "Clawd"라는 픽셀 크랩 마스코트가 실제로 존재하며, 공개적으로 알려진 특징은 주황색 계열 몸통·블록형 픽셀 아트·각진 눈이라는 것뿐이었다. 이 특징만 참고해 `assets/claude-mascot/claude-mascot-{1..5}.png` 5종(표정만 다름)을 직접 그려 넣었다.
  - ⚠️ 개정(2026-09-17, 이미지 파일로 교체 2차, 최종): 사용자가 직접 준비한 이미지 파일을 `assets/claude-icon.png`로 제공해, 위 자체 제작 5종을 이 파일 1장으로 완전히 교체했다(기존 `assets/claude-mascot/` 폴더와 5종 파일은 삭제). 표정 변형은 더 이상 없고, 매번 이 이미지 1장을 그대로 쓴다. 원본 비율(492×322, 가로가 긴 직사각형)이 정사각형이 아니므로, 찌그러지지 않게 아이콘 표시 폭만 고정하고 높이는 원본 비율에 맞춰 자동 계산되도록 CSS를 조정했다.
  - 도형/SVG가 아닌 이미지 파일을 쓰는 예외 케이스지만, 이 프로젝트가 이미 `assets/image{N}/`에 실제 이미지 파일을 쓰고 있어("틀린 그림 찾기" 본판) 구조적으로 문제되지 않는다.
- **클릭(잡기) 반응** (Q&A로 확정): 지나가는 클로드 아이콘을 클릭하면 그 즉시
  1. 남은 시간이 3초 증가한다 (단, 해당 단계 제한시간을 넘지 않도록 상한 클램프 — 오답 페널티가 즉시 반영되는 것과 대칭으로, 이 보너스도 다음 타이머 tick을 기다리지 않고 즉시 반영한다).
  2. 클릭한 자리에 "+3초" 같은 짧은 텍스트가 잠깐 떴다 사라지는 시각 피드백을 준다.
  3. 그 아이콘은 즉시 화면에서 사라진다(DOM에서 제거) — 이미 사라진 아이콘은 클릭할 수 없으므로 중복 반영(멱등 처리)이 자연히 보장된다.
  - 새 소리 효과는 추가하지 않는다(시각 전용 보너스). 따라서 음소거 토글과는 무관하다.
- **움직임 감소 토글과의 관계**: 이 이벤트 전체(아이콘이 날아가는 것 자체)가 움직임에 기반하므로, 움직임 감소가 켜져 있으면 클로드 아이콘을 아예 스폰하지 않는다. 토글을 켜는 순간 이미 날고 있는 아이콘도 모두 즉시 제거한다("+3초" 클릭 피드백도 사라지는 연출이 애니메이션이므로 같은 원칙 적용). 정적 표시로 대체하지 않는다(움직이며 지나가는 것 자체가 이 효과의 핵심이라 대체 의미가 없다고 판단).
- **정리(cleanup)**: 단계 전환/재시작/일시정지/게임 종료 시 예약된 스폰 타이머(`setTimeout`)와 현재 날고 있는 아이콘의 DOM 요소·타이머를 모두 정리한다(요구사항 2단계 cleanup 원칙 적용). 일시정지 중에는 새 스폰을 멈추고, 일시정지 시점에 떠 있던 아이콘은 제거한다(정확한 애니메이션 진행률 보존은 하지 않음 — 장식용 효과이므로 재개 후 새로 스폰).
- **이벤트 리스너**: 아이콘 DOM 요소를 생성할 때마다 그 요소에 `click` 리스너를 1개만 등록한다(정답 클릭 폭죽 파티클과 동일한 패턴 — 요소가 그때그때 새로 생성·삭제되므로 "요소당 리스너 1개" 원칙을 인스턴스 단위로 지킨다).
- **데이터 분류**: 날고 있는 클로드 아이콘의 존재/위치/타이머는 요구사항 4단계 기준 "현재 판 값"(완전히 메모리상 장식 효과)이며 저장 대상이 아니다. 새로고침/재시작 시 당연히 사라진다.

### 통과 기준 → 설계 매핑 (자체 점검용)
- T02-C26: 성공 이벤트 핸들러 내부에서만 효과 트리거 호출, 다른 상태 변화 경로에서 호출되지 않도록 단일 진입점 유지
- T02-C27: 음소거/움직임 감소 토글 변경 시 진행 중인 오디오 재생을 즉시 정지(`pause()`)하고 진행 중인 애니메이션을 즉시 취소(`cancelAnimation`/클래스 제거)
- T02-C28 / T02-C29: 이름 입력은 표시용 별명만 허용, 개인정보 유도 문구 없음. 비밀값/API 키를 아예 사용하지 않는 클라이언트 전용 구조 유지, 배포 전 문자열 검색으로 재확인
- T02-C30 / T02-C31: 게임 스펙과 별개로 제출 시점에 별도 문서로 작성 (백로그)
- 클로드 아이콘 미니 이벤트: 스폰 타이머/아이콘 요소는 일시정지·단계전환·재시작·움직임 감소 토글 On 시 즉시 정리, 잡기 반응의 시간 증가는 오답 페널티와 동일하게 `tick()`을 즉시 호출해 다음 tick을 기다리지 않고 반영, 제한시간 초과로 증가하지 않도록 클램프

### 막힘 방지 체크리스트
- 효과가 계속 반복되면 확인 순서:
  1. 이벤트 발생 조건이 실제로 1회성인지(중복 호출 경로가 없는지) 확인
  2. 이전 효과(사운드/애니메이션)가 제대로 종료됐는지 확인
  3. 음소거/움직임 감소 등 공통 설정값이 효과 트리거 함수에 제대로 반영되는지 확인

---

## 요구사항 5단계까지 모두 확정됨
1~5단계 요구사항이 모두 확정되었다. 이후 실제 구현 작업은 이 파일 전체를 기준으로 진행하며, 새로운 요구사항이나 변경 지시가 오면 해당 섹션을 개정하고 상단 "⚠️ 개정" 표시를 남긴다.
