# 프로젝트: 틀린 그림 찾기 미니 게임

## 기술 스택
- 순수 HTML/CSS/JavaScript (바닐라, 프레임워크·빌드 도구 없음)
- 정적 파일만으로 구성, 백엔드/서버 없음. 데이터 보존은 브라우저 `localStorage`만 사용
- 이유: T02-C01(계정/인증 없이 시크릿 창에서 즉시 열림)에 가장 잘 맞고, 가벼운 이미지 기반 게임이라 프레임워크가 불필요함

## 반드시 먼저 확인할 것
게임 규칙·사양의 단일 진실 공급원은 **[GAME_RULES.md](GAME_RULES.md)** 이다.
이 프로젝트에서 게임 로직/화면/흐름을 구현, 수정, 리뷰하기 전에 항상 `GAME_RULES.md`를 먼저 읽고 그 내용과 어긋나지 않게 작업한다.

## 요구사항 진행 방식
- 요구사항 1~5단계가 모두 확정되어 `GAME_RULES.md`에 정리되어 있다. 실제 구현은 이 파일 전체를 기준으로 진행한다.
- 새로운 요구사항이나 사양 변경 지시가 오면, 구현/수정 전에 반드시 AskUserQuestion으로 애매한 지점을 먼저 확인한 뒤 `GAME_RULES.md`의 해당 섹션을 개정한다.
- 이전에 확정된 사양은 명시적인 변경 지시가 없는 한 그대로 유지한다.

## 통과 기준(T02-C0x) 관련 원칙
- 모든 결과물 URL은 계정 생성/로그인/인증/초대/비밀번호/OAuth/CAPTCHA 없이 시크릿 창에서 바로 열려야 한다 (T02-C01).
  - 이름 입력 같은 단순 텍스트 입력은 허용되지만, 실제 계정/로그인 시스템은 절대 추가하지 않는다.
  - 랭킹/기록 저장이 필요한 경우 서버·백엔드·계정 시스템 대신 브라우저 `localStorage`를 우선 사용한다.
- 게임 규칙과 핵심 조작은 항상 공개 화면에 노출한다 (T02-C03, T02-C04).
- 현재 게임 상태(단계, 남은 시간, 진행도 등)는 항상 화면에 표시한다 (T02-C05).
- 핵심 조작 1회는 상태 변화 1회로 반영되어야 한다 (T02-C06).
- 성공/실패 후 재시작 시 현재 판 상태는 반드시 초기화되어야 한다 (T02-C08, T02-C09).

## 오답 페널티 원칙 (추가 요청으로 확정)
- 오답 클릭 1회당 남은 시간에서 즉시 10초를 차감한다. 차감 후 남은 시간이 0 이하가 되면 그 즉시 실패 처리한다 (다음 타이머 tick까지 기다리지 않음).
- 각 단계 시작 시 오답 누적 차감량은 0으로 초기화한다.

## 비주얼 원칙
- ⚠️⚠️⚠️ 개정(2026-09-16, 최신): 픽셀 단위 자동 탐지 방식도 폐기되었다. 상세 사양은 `GAME_RULES.md`의 "하드코딩 좌표 기반 전환 (3차, 현재 버전)" 섹션이 유일한 현재 기준이다.
- 각 이미지 세트는 `assets/image숫자/` 폴더에 있으며, `image숫자.png`(기준, 좌측 고정)와 `image숫자-{1,2,3}.png`(오답 3종, 매 단계 랜덤 1개를 우측에 고정 배치)로 구성된다.
- 다른점 좌표는 사람이 육안으로 확인해 같은 폴더의 `diffs.js`에 비율(0~1) 좌표(`{x,y,radius}`)로 미리 정의해둔다. 게임 실행 중에는 픽셀 비교를 하지 않고 이 값을 그대로 읽어 쓴다. 다른점 개수는 `diffs.js`에 정의된 배열 길이 그대로다. 찾음 표시는 얇은 원형 테두리(링)만 사용한다.
- `diffs.js`는 순수 JSON이 아니라 `<script src>`로 로드되는 JS 파일이다 (JSON+`fetch()`는 `file://`에서 로컬 파일 접근이 차단되어 T02-C01의 "서버 없이 시크릿 창에서 바로 열림"을 깨기 때문). 이미지 세트를 추가할 때는 이미지 파일과 함께 `diffs.js`도 반드시 같이 추가해야 하며, 없거나 스키마가 틀리면 그 세트는 조용히 목록에서 제외된다.
- `radiusRange`(단계별 반지름 랜덤화)나 픽셀 임계값/최소 면적/병합 거리 같은 자동 탐지 파라미터 개념은 모두 폐기되었다. 반지름은 `diffs.js`에 적힌 값을 그대로 쓴다.

## 이미지 자산 배정 원칙 (추가 요청으로 확정, 폴더 단위)
- 페이지 로드 시 `assets/image1/`, `image2/`, ... 순서로 존재 여부를 자동 프로브하여 사용 가능한 이미지 세트 목록을 만든다 (폴더 이름/개수 하드코딩 없음). **단계 수 = 이때 감지된 이미지 세트 개수**이며, 현재는 `image1`만 있어 실질적으로 1단계로 끝난다. `image2` 이상 폴더를 추가하면 코드 수정 없이 다음 새 게임부터 단계 수가 늘어난다.
- 바로 이전 단계에서 사용한 이미지 세트(폴더)는 이번 단계에 다시 배정하지 않는다.
- 각 단계 시작 시 이번 게임에서 아직 사용하지 않은 세트 중에서 무작위로 배정한다. 모두 사용했다면 "바로 직전 단계만 아니면 됨" 조건으로 완화해서 계속 무작위 배정한다 (셔플백 방식).
- 사용 가능한 세트가 1개뿐이면 위 규칙을 적용할 수 없으므로 예외적으로 재사용을 허용한다.
- 이미지 세트 사용 이력은 "현재 판 값"이라 저장하지 않고, 새 게임을 시작할 때마다 초기화한다.

## 난이도 튜닝 원칙 (요구사항 3단계 기준)
- 난이도 관련 값(제한시간/오답 페널티/다른점 판정 임계값 등)을 바꿀 때는 감으로 정하지 않고, 변경 전 10회·변경 후 10회 플레이 기록(소요시간, 성공/실패, 실패 원인)을 비교해서 정한다.
- 한 번의 비교 라운드에서는 파라미터를 **정확히 1개만** 바꾸고 나머지는 두 묶음에서 완전히 동일하게 고정한다 (T02-C20). 여러 값을 동시에 바꾸는 튜닝은 금지.
- 순위/점수는 소요시간(초) 총합이 적을수록 높은 순위로 계산한다. 실패한 판은 해당 단계 제한시간 전체를 소요시간으로 간주한다.
- 최종값 선택 이유는 항상 기록의 중앙값/범위 비교 또는 반복되는 실패 원인과 연결지어 남긴다 (T02-C21).

## 견고성 원칙 (요구사항 2단계 기준)
- 레이아웃은 고정 두 세트가 아니라 %, vw/vh 기반의 **유동형 단일 레이아웃**으로 구현하여 1366×768 / 1920×1080 모두에서 잘림·가로 스크롤이 없어야 한다.
- 인터랙티브 요소마다 이벤트 리스너는 정확히 1개만 등록한다 (중복 바인딩 금지). 이미 처리된 대상에 대한 재입력은 상태를 추가로 바꾸지 않는다(멱등 처리).
- 타이머/일시정지는 오직 명시적 "일시정지" 버튼으로만 제어한다. `visibilitychange`/`blur`/`focus` 같은 포커스 이벤트에 타이머나 게임 상태를 연동하지 않는다 (포커스 이탈과 무관하게 타이머는 계속 진행).
- 창 리사이즈 핸들러는 CSS/레이아웃만 재계산하고 게임 상태(state)는 절대 건드리지 않는다.
- 컴포넌트/단계 전환 시 이전에 등록한 `setInterval`/`setTimeout`/이벤트 리스너를 반드시 정리(cleanup)하여 장시간 실행 시 누적·콘솔 에러가 없도록 한다.

## 데이터 처리 원칙 (요구사항 4단계 기준)
- "현재 판 값"(단계/타이머/찾은개수/이미지 배치/일시정지 등)은 메모리 상태로만 관리하고 절대 저장하지 않는다. 새 게임 시작이나 새로고침/재접속 시 항상 초기값으로 재생성한다.
- "보존 기록"은 순위(랭킹) 기록 하나뿐이며 `localStorage`에만 저장한다. 5단계 클리어로 최종 기록이 확정되는 시점 외에는 절대 갱신하지 않는다.
- `localStorage`를 읽을 때는 항상 try/catch와 스키마 검증(타입·필수 필드)을 거친다. 키가 없거나(빈 값) 파싱/검증에 실패하면(손상값) 예외를 던지지 말고 선언된 기본값(`{version:1, records:[]}`)으로 폴백한 뒤 정상 진행한다.
- 손상된 저장값을 감지하면 기본값으로 즉시 재저장하여 다음 접속부터 정상 상태가 유지되게 한다.

## 이벤트 효과 및 접근성 원칙 (요구사항 5단계 기준)
- 단계 성공 시 소리(짧은 비프/차임)와 애니메이션(좌우 판 캔버스 번쩍임) 효과를 정확히 1회만 재생한다. 실패·오답 클릭 효과는 백로그(추후 같은 방식으로 확장).
- 효과는 선언한 이벤트의 핸들러 안에서만 트리거한다. 다른 코드 경로에서 우연히 다시 호출되지 않도록 단일 진입점을 유지한다.
- 홈 화면에 음소거 토글과 움직임 감소 토글을 각각 상시 노출한다. 토글을 켜는 즉시 재생 중인 해당 효과(소리는 정지, 애니메이션은 취소)를 멈추고, 이후 발생하는 이벤트에서도 재생하지 않는다.
- 이 두 토글의 설정은 세션 내 유지로 충분하며, 요구사항 4단계의 `localStorage` 보존 대상(순위 기록)에는 포함하지 않는다.

## 개인정보/비밀값 원칙 (요구사항 5단계 기준)
- 플레이어 이름은 표시용 별명으로만 취급하고, 실명·이메일·연락처 등 실제 개인정보를 입력·저장·출력하지 않는다.
- 이 프로젝트는 백엔드/외부 API가 없으므로 API 키·토큰 등 비밀값을 코드에 절대 하드코딩하지 않는다.
- 배포/제출 전에는 소스 전체에서 `key`/`token`/`secret`/`password` 등을 검색해 비밀값이 없는지 재확인한다.
- 제출용 문서(짧은 확인 방법, AI 사용 내역 고지)는 게임 스펙과 무관한 별도 산출물이므로 이 저장소의 스펙 파일에는 포함하지 않는다.

## 막힘 발생 시 점검 순서
조작/버튼이 반응하지 않으면: 입력 포커스 확인 → 이벤트 리스너 중복 등록 확인 → 상태·타이머 초기화 확인
포커스 복귀 직후 실패/입력 중복이 발생하면: 이탈 중 타이머 정지 여부 → 재개 기준 시각 계산 → 이벤트 누적 여부 확인
