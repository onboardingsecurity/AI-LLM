# CLAUDE.md

## 프로젝트 개요
이미지와 문구를 조합해 밈·카드·SNS 게시용 이미지를 만들고 파일로 내려받는 제작 도구.

## 작업 규칙
- 기술 스택: 순수 웹 앱(HTML, JavaScript, Canvas). 서버 없이 정적 파일로 배포한다.
- 공개 위치: 결과물은 Vercel, 소스는 공개 GitHub 저장소. 로그인 없이 열려야 한다.
- 템플릿 저장: IndexedDB에 이미지 본체까지 저장한다.
- 내보내기: 1080x1080(1:1), 1080x1350(4:5), 1080x1920(9:16), PNG.
- C29(개인정보 점검, git 커밋 이메일 포함), C30(비밀값 점검), C31(짧은 확인 방법), C32(제출문)는 사용자가 직접 확인하거나 작성한다. Claude는 C31, C32를 작성하지 않고, 점검 때 C29~C32를 사용자 담당으로 표시만 한다. git 설정은 Claude가 바꾸지 않는다. (C29, C30은 사용자가 담당으로 정했다. Claude가 참고로 검색한 결과는 근거로 삼지 않는다.)
- C27(이미지 출처 기록)은 CREDITS.md에 이미지별로 기록한다. 본인 제작 여부, 원본 출처 URL, 사용 허가 근거를 적는다.
- 요구사항은 1~5단계로 나뉘어 있다. 매 작업마다 해당 단계의 "통과 기준"을 점검하고, 결과를 근거와 함께 보고한다.
- 통과 기준 ID(T03-Cxx)는 원문 그대로 유지한다. 임의로 바꾸거나 합치거나 빈 번호를 채우지 않는다.
- 모르는 것이나 선택이 필요한 것은 추측하지 말고 사용자에게 묻는다.
- 미정 항목은 `[미정]`으로 표시하고, 답을 받으면 이 문서를 갱신한다.

## 프로젝트 구조와 실행
- index.html, css/style.css, js/app.js: 앱 본체. 빌드 없이 정적 파일로 동작한다.
- fonts/: 웹폰트(Gamja Flower 400, 화면 전체와 카드 문구에 사용)와 OFL 라이선스 전문. 변환 방법은 tools/make_font.sh.
- assets/left.png: 미리보기 창(회색 박스, .canvas-wrap) 바깥 왼쪽 여백의 아래쪽에 서 있는 캐릭터(index.html의 `.canvas-wrap > img.deco-left`, css/style.css). 크기는 창 왼쪽 바깥에 남는 폭에 맞춰 커지고(최대 원본 500px 기준), 창 폭 1280px 미만에서는 숨긴다. 500x500 안에 투명 여백이 있어서 그 비율(0.678)로 크기와 위치를 계산한다.
- assets/templates/template-image1~3.png: 기본 템플릿 3개의 배경 이미지(1796×876, 넓은 배경 가운데에 캐릭터). 생성 정보(C2PA)는 제거했고 화소는 그대로다. 출처는 CREDITS.md.
- assets/right.png: 맨 오른쪽 패널(템플릿) 바깥 오른쪽 여백의 아래쪽에 서 있는 캐릭터(index.html의 `.tool-template > .deco-right`, css/style.css). 왼쪽 캐릭터(left.png)와 같은 규칙으로 크기와 위치를 정하고, 그림에서 캐릭터 부분(560x970)만 잘라 보여 준다. 창 폭 1280px 미만에서는 숨긴다. 원본에 든 생성 정보(C2PA)는 제거한 사본이다.
- assets/mouse-cursor.png: 서비스 전체에서 쓰는 마우스 커서(css/style.css의 `* { cursor: ... }`, 클릭 위치는 이미지 위쪽 가운데 4,0). 요소별 커서(grab, pointer 등)는 모두 이 이미지로 통일했다. 22×33이라 일부 브라우저는 창 가장자리에서 기본 커서로 바뀔 수 있다.
- assets/background.png: 서비스 페이지 배경(CSS body::before). ChatGPT로 생성한 이미지에서 생성 정보(C2PA)를 제거한 사본이고 출처는 CREDITS.md. 카드 결과물의 배경이 아니다.
- samples/final/: 완성 이미지 3개(card-1x1.png, card-4x5.png, card-9x16.png). 앱에서 실제로 내려받아 만들었다. samples/backgrounds/: 그 배경(tools/make_backgrounds.py로 생성). samples/import-tests/: 가져오기 시험용 JSON 3종(이 앱에서 내보낸 것).
- tools/make_test_images.py: 시험용 합성 이미지 생성 스크립트.
- tools/make_backgrounds.py: 카드 배경 3장(samples/backgrounds/, 화면비와 같은 크기의 분필 낙서 배경) 생성 스크립트. 완성 이미지가 이 배경을 쓴다.
- samples/test-inputs/: 정상 PNG, 정상 JPEG, 투명 PNG, 잘못된 파일 3종.
- CREDITS.md: 글꼴과 이미지의 출처, 허가 근거.
- tests/: 없음. 1~5단계 자동 점검 스크립트(Playwright)는 사용자 요청으로 저장소에서 삭제했다. docs/의 결과 기록은 삭제 전에 돌린 결과이고 이제 자동으로 다시 만들 수 없다. 앱을 고치면 결과가 어긋날 수 있으니 그때는 화면에서 직접 확인하거나 점검 코드를 다시 만든다.
- docs/: 2단계 화면/파일 대조 결과(stage2-compare.md와 비교 이미지 3장), 3단계 검사표와 수정 전후 기록(stage3-checks.md, stage3-compare-*.png), 4단계 결과(stage4-results.md), 5단계 결과(stage5-import-results.md, stage5-final-images.md), 템플릿 데이터 구조와 JSON 파일 형식(template-schema.md, 손으로 관리).
- 로컬 실행: 저장소 루트에서 `python3 -m http.server 8765` 후 http://localhost:8765/ 접속.
- 내려받기는 미리보기 캔버스를 그대로 저장하지 않고, 같은 render()로 새 캔버스에 다시 그려 저장한다. 저장 전에 글꼴 로딩을 기다린다.
- 그리기는 js/app.js의 render() 하나로 한다. 미리보기와 이후 내보내기가 이 함수를 함께 쓴다. 상태(state)가 원본이고 화면은 그 결과다.

## 진행 상황
- 1단계: 구현과 자동 점검 완료. C01은 사용자가 배포한 뒤 시크릿 창으로 확인해야 한다.
- 2단계: 구현과 자동 점검 완료(C11~C13). 화면비 3종 선택, PNG 내려받기, 대조 결과는 docs/stage2-compare.md.
- 3단계: 구현과 자동 점검 완료(C14~C16). 검사 12건 중 수정 전 7건 FAIL → 수정 후 12건 PASS. 결과는 docs/stage3-checks.md. 예상값은 코드 수정 전에 고정했고 docs/stage3-checks.md 표에 남아 있다(스크립트는 삭제).

- 5단계: Claude 담당 부분 완료(C22~C28). JSON 가져오기는 docs/stage5-import-results.md, 완성 이미지 3개(samples/final/)는 docs/stage5-final-images.md. 남은 것은 사용자 담당(C01, C29~C32).
- 4단계: 구현과 자동 점검 완료(C17~C21). 결과는 docs/stage4-results.md, 데이터 항목 정의는 docs/template-schema.md.

---

## 1단계: 첫 편집 흐름 (이미지 불러오기, 한글 문구 편집, 잘못된 파일 처리)

### 하는 일
이미지를 불러와 한글 문구의 위치, 크기, 색을 바꾸고 즉시 미리 본다.

### 완료 목표
PNG, JPEG, 잘못된 파일을 안전하게 처리하는 첫 편집 흐름을 만든다.

### 첫 행동
1. 사용 권한을 확인하고 메타데이터를 제거한 합성 이미지를 준비한다.
2. PNG, JPEG, 지원하지 않는 파일을 각각 불러온다.

### 통과 기준
| ID | 기준 | 검증 방법 |
|---|---|---|
| T03-C01 | 제출한 모든 URL(결과물, 소스)은 계정 생성, 로그인, 인증, 초대, 비밀번호, OAuth, CAPTCHA 없이 새 시크릿 창에서 열린다. | 각 URL을 새 시크릿 창에서 열어 바로 보이는지 확인 |
| T03-C03 | 이미지 편집 도구와 문구 편집 도구가 첫 화면에 보인다. | 첫 화면 로드 직후 스크롤 없이 두 도구가 보이는지 확인 |
| T03-C04 | PNG 이미지를 불러올 수 있다. | PNG 불러오기 후 미리보기 표시 확인 |
| T03-C05 | JPEG 이미지를 불러올 수 있다. | JPEG 불러오기 후 미리보기 표시 확인 |
| T03-C06 | 문구 위치 변경이 미리보기에 즉시 반영된다. | 위치 조작 직후 미리보기 변화 확인 |
| T03-C07 | 문구 크기 변경이 미리보기에 즉시 반영된다. | 크기 조작 직후 미리보기 변화 확인 |
| T03-C08 | 문구 색 변경이 미리보기에 즉시 반영된다. | 색 조작 직후 미리보기 변화 확인 |
| T03-C09 | 지원하지 않는 파일을 넣어도 기존 작업이 유지된다. | 작업 중 미지원 파일 투입 전후로 이미지, 문구, 위치, 크기, 색 대조 |
| T03-C10 | 지원하지 않는 파일의 거부 이유가 표시된다. | 미지원 파일 투입 시 화면에 이유 메시지 표시 확인 |

참고: 원문에 T03-C02는 원래 없다. 결번이며 확인할 필요 없다.

### 막히는 지점과 확인 순서
- 문제: 이미지나 문구가 보이지 않는다.
- 확인 순서: 파일 읽기 완료 -> 미리보기 크기 -> 입력 변경 후 다시 그리기

### 남길 것 (산출물)
- PNG, JPEG 편집 기능
- 잘못된 파일 거부 기능

### 설계 제약 (통과 기준에서 도출)
- 불러오기는 "읽기/검증 -> 성공 시에만 교체" 순서로 한다. 실패하면 현재 작업 상태를 건드리지 않는다. (C09)
- 파일 형식은 확장자가 아니라 실제 내용(시그니처)과 브라우저 디코딩 성공 여부로 판별한다. (C04, C05, C09)
- 거부 이유는 사용자가 읽을 수 있는 문장으로 화면에 표시한다. (C10)
- 문구의 위치, 크기, 색 입력이 바뀔 때마다 미리보기를 다시 그린다. 이미지 로드 완료 후에만 그린다. (C06~C08, 막히는 지점)
- 결과물과 소스 URL은 로그인 없이 공개되어야 한다. 시크릿 창에서 실제로 열어 확인한다. (C01)
- 시험용 이미지는 사용 권한을 확인하고 메타데이터를 제거한 합성 이미지를 쓴다. (첫 행동)

### 매 작업 점검 체크리스트 (1단계)
- [ ] C01 모든 제출 URL이 시크릿 창에서 로그인 없이 열림
- [ ] C03 이미지, 문구 편집 도구가 첫 화면에 보임
- [ ] C04 PNG 불러오기
- [ ] C05 JPEG 불러오기
- [ ] C06 문구 위치 즉시 반영
- [ ] C07 문구 크기 즉시 반영
- [ ] C08 문구 색 즉시 반영
- [ ] C09 미지원 파일 투입 후 기존 작업 유지
- [ ] C10 미지원 파일 거부 이유 표시

### 미정 항목 (사용자 확인 필요)
- [확정] 기술 스택: 순수 웹 앱 (HTML, JavaScript, Canvas). 서버와 빌드 도구 없이 정적 파일로 만든다.
- [해소] T03-C02는 원문에 원래 없는 번호다. 결번으로 두고 신경 쓰지 않는다.
- [확정] 공개 위치: 결과물은 Vercel, 소스는 공개 GitHub 저장소. 서비스는 Vercel로 정했다.
  - 주의: Vercel의 배포 보호(로그인 요구) 설정이 켜져 있으면 C01 위반이다. 제출 전에 시크릿 창으로 결과물 URL과 소스 URL을 실제로 열어 확인한다.
- [확정] 조작 방식: 위치는 미리보기에서 드래그, 크기는 슬라이더, 색은 색 선택기.
- [확정] 한글 폰트: 라이선스가 자유로운 웹폰트를 저장소에 파일로 포함한다. (처음에는 Noto Sans KR 700이었으나, 사용자 요청으로 Gamja Flower 400으로 교체했다. 굵기는 400 한 가지뿐이라 캔버스도 400으로 그린다.) 폰트 출처와 라이선스는 CREDITS.md에 적는다.

---

## 2단계: 화면비별 미리보기와 내려받은 파일의 일치

### 하는 일
세 화면비에서 미리보기와 내려받은 파일을 대조한다.

### 완료 목표
1:1, 4:5, 9:16에서 이미지와 문구의 배치를 미리보기와 파일이 같게 만든다.

### 첫 행동
1. 세 비율 각각에 가장자리와 줄바꿈이 보이는 검사 문구를 배치한다.
2. 미리보기와 내려받은 파일을 나란히 비교한다.

### 통과 기준
| ID | 기준 | 검증 방법 |
|---|---|---|
| T03-C11 | 1:1 미리보기와 내려받은 파일의 이미지 잘림, 문구 위치, 줄바꿈이 일치한다. | 두 결과를 나란히 놓고 세 항목 대조 |
| T03-C12 | 4:5 미리보기와 내려받은 파일의 이미지 잘림, 문구 위치, 줄바꿈이 일치한다. | 위와 동일 |
| T03-C13 | 9:16 미리보기와 내려받은 파일의 이미지 잘림, 문구 위치, 줄바꿈이 일치한다. | 위와 동일 |

세 기준의 공통 비교 항목은 다음 세 가지다.
- 이미지 잘림: 어느 부분이 잘려 나갔는가
- 문구 위치: 이미지 대비 상대 위치가 같은가
- 줄바꿈: 같은 글자에서 줄이 바뀌는가

### 막히는 지점과 확인 순서
- 문제: 저장 파일의 글자 위치가 미리보기와 다르다.
- 확인 순서: 가로, 세로 크기 -> 좌표 기준 -> 글꼴 준비 뒤 저장

### 남길 것 (산출물)
- 세 화면비의 화면/파일 대조 결과

### 설계 제약 (통과 기준에서 도출)
- 미리보기와 내보내기가 같은 그리기 코드와 같은 좌표계를 쓴다. 배율만 다르게 하고, 좌표는 캔버스 크기에 대한 비율로 저장한다. (C11~C13)
- 화면비별 캔버스의 가로, 세로 크기를 먼저 확정하고, 그 크기 기준으로 그린다. (막히는 지점 1번)
- 이미지 잘림(맞춤 방식)과 줄바꿈 계산도 미리보기와 내보내기에서 같은 로직을 쓴다. 줄바꿈은 같은 글꼴, 크기, 폭 기준으로 계산한다. (C11~C13)
- 내보내기 전에 글꼴 로딩 완료를 기다린다. 로딩 전에 저장하면 대체 글꼴로 그려져 위치와 줄바꿈이 달라진다. (막히는 지점 3번)
- 검사 문구는 가장자리 위치와 줄바꿈 지점이 눈에 띄게 만든다. 세 비율 모두에서 같은 문구로 대조한다. (첫 행동)

### 매 작업 점검 체크리스트 (2단계)
- [ ] C11 1:1 잘림, 위치, 줄바꿈 일치
- [ ] C12 4:5 잘림, 위치, 줄바꿈 일치
- [ ] C13 9:16 잘림, 위치, 줄바꿈 일치
- [ ] 세 비율의 화면/파일 대조 결과를 남김

### 미정 항목 (사용자 확인 필요)
- [확정] 내보내기: 1:1은 1080x1080, 4:5는 1080x1350, 9:16은 1080x1920. 파일 형식은 PNG만 쓴다.
- [확정] 이미지 맞춤: 요구사항에 명시는 없다. 통과 기준이 "이미지 잘림 일치"를 비교하므로 캔버스를 꽉 채우고 넘치는 부분을 중앙 기준으로 잘라낸다.
- [확정] 대조 결과: 비율마다 미리보기와 내려받은 파일을 나란히 놓은 비교 이미지와, 세 항목 일치 여부를 적은 표를 docs/ 폴더에 남긴다.

## 3단계: 극단 입력에서 편집 내용 보존과 결함 수정 기록

### 하는 일
긴 글, 혼합 문자, 이모지, 다양한 이미지에서 편집 내용을 보존한다.

### 완료 목표
12개 검사와 대표 결함의 수정 전후를 남긴다.

### 첫 행동
1. 예상값이 있는 극단 입력 12개를 정한다.
2. 미리보기와 파일을 검사하고 대표 결함을 고친다.

### 통과 기준
| ID | 기준 | 검증 방법 |
|---|---|---|
| T03-C14 | 검사 기록이 12건 있다. 입력에 긴 한글, 영문 혼합, 줄바꿈, 이모지, 빈 문구, 세로 이미지, 가로 이미지, 투명 이미지가 포함된다. | 12건 표에서 여덟 가지 입력 종류가 모두 들어 있는지 대조 |
| T03-C15 | C14의 12건 중 한 건 이상에 수정 전 FAIL과, 같은 검사 입력의 수정 후 PASS가 있다. | 같은 입력으로 수정 전 FAIL 기록과 수정 후 PASS 기록이 짝으로 존재하는지 확인 |
| T03-C16 | 잘못된 극단 입력 뒤 기존 편집 내용이 예고 없이 사라지지 않는다. | 극단 입력 투입 전후로 이미지, 문구, 위치, 크기, 색 대조 |

C14의 입력 종류 여덟 가지: 긴 한글 / 영문 혼합 / 줄바꿈 / 이모지 / 빈 문구 / 세로 이미지 / 가로 이미지 / 투명 이미지.
12건은 이 여덟 가지를 모두 포함하고, 나머지는 조합이나 변형으로 채운다. 각 검사는 예상값을 미리 적는다.

### 막히는 지점과 확인 순서
- 문제: 긴 문장이나 이모지가 잘린다.
- 확인 순서: 사용 폭 확인 -> 줄바꿈과 대체 글꼴 -> 영역 높이 확인

### 남길 것 (산출물)
- 12개 검사표
- 대표 결함의 수정 전후 기록

### 설계 제약 (통과 기준에서 도출)
- 검사 12건은 코드를 고치기 전에 입력과 예상값을 먼저 확정한다. 결과를 본 뒤에 예상값을 바꾸지 않는다. (C14, C15)
- 각 검사는 미리보기와 내려받은 파일을 둘 다 확인한다. (첫 행동)
- 수정 전 FAIL은 실제로 돌려서 기록한다. 같은 입력으로 수정 후 PASS를 기록해 짝을 맞춘다. FAIL이 없으면 수정 전후 기록이 성립하지 않는다. (C15)
- 문구가 긴 경우에도 글자를 조용히 잘라 내지 않는다. 줄바꿈, 글꼴 축소, 영역 확장, 경고 중 하나로 처리한다. (막히는 지점)
- 이모지와 혼합 문자는 대체 글꼴이 적용되는 것을 전제로 폭과 높이를 계산한다. (막히는 지점 2번)
- 투명 이미지는 PNG와 JPEG 내보내기에서 배경 처리가 어떻게 되는지 예상값에 적어 둔다.
- 빈 문구, 지원하지 않는 이미지, 비정상 입력이 들어와도 현재 편집 상태를 유지한다. 버리려면 먼저 사용자에게 알린다. 1단계 C09의 방식과 같은 원칙이다. (C16)

### 매 작업 점검 체크리스트 (3단계)
- [x] C14 검사 12건, 여덟 가지 입력 종류 포함
- [x] C14 각 검사에 예상값이 사전에 적혀 있음
- [x] C15 수정 전 FAIL과 수정 후 PASS가 같은 입력으로 짝을 이룸
- [x] C16 극단 입력 뒤 기존 편집 내용 유지
- [x] 12개 검사표와 대표 결함 전후를 산출물로 남김

### 미정 항목 (사용자 확인 필요)
- [확정] 긴 문구 처리: 폭(캔버스 폭의 90%)에 맞춰 자동 줄바꿈하고, 높이(캔버스 높이의 90%)를 넘으면 그릴 때만 글꼴을 줄인다. 슬라이더 값(state.size)은 바꾸지 않는다. 줄인 경우 화면에 알림을 띄운다. 최소 크기까지 줄여도 넘치면 경고한다. 미리보기와 내보내기가 같은 render()에서 계산한다.
- [확정] 투명 이미지: PNG로 내려받을 때 투명 영역을 투명 그대로 유지한다(배경색을 채우지 않는다).
- [확정] 검사표와 수정 전후 기록: docs/stage3-checks.md + 스크린샷. (자동 실행 스크립트 tests/는 나중에 삭제했다.)
- [확정] "잘못된 극단 입력" 범위: 검사 12건 모두. 각 검사 뒤에 지원하지 않는 파일을 넣어 기존 편집 내용이 유지되는지도 확인한다.

## 4단계: 템플릿 CRUD와 새로고침 유지

### 하는 일
사용자가 템플릿을 만들고, 불러오고, 고치고, 지우게 한다.

### 완료 목표
템플릿 3개 이상의 생성, 불러오기, 수정, 삭제(CRUD)와 새로고침 유지를 확인한다.

### 첫 행동
1. 템플릿 데이터 항목을 정하고 세 개를 만든다.
2. 하나를 수정하고 하나를 삭제한 뒤 새로고침한다.

### 통과 기준
| ID | 기준 | 검증 방법 |
|---|---|---|
| T03-C17 | 사용자가 템플릿을 3개 이상 생성할 수 있다. | 세 개 이상 만들어 목록에 모두 보이는지 확인 |
| T03-C18 | 생성한 템플릿을 다시 불러올 수 있다. | 템플릿을 불러와 저장한 편집 내용이 복원되는지 확인 |
| T03-C19 | 생성한 템플릿을 수정할 수 있다. | 한 개를 수정하고 그 템플릿만 바뀌었는지 확인 |
| T03-C20 | 생성한 템플릿을 삭제할 수 있다. | 한 개를 삭제하고 그 템플릿만 사라졌는지 확인 |
| T03-C21 | 템플릿 변경 결과가 새로고침 뒤 유지된다. | 수정과 삭제 후 새로고침해서 결과가 그대로인지 확인 |

### 막히는 지점과 확인 순서
- 문제: 수정, 삭제 대상이 바뀐다.
- 확인 순서: 안정된 ID 사용 -> 현재 배열 위치 사용 금지 -> 저장 데이터로 다시 그리기

### 남길 것 (산출물)
- 템플릿 CRUD 기능
- 재접속(새로고침) 결과

### 설계 제약 (통과 기준에서 도출)
- 템플릿마다 생성 시 고유 ID를 부여하고, 수정과 삭제는 ID로만 대상을 찾는다. 배열 인덱스나 화면 순서로 찾지 않는다. (막히는 지점)
- 화면은 저장된 데이터를 기준으로 다시 그린다. 화면 상태를 원본으로 삼지 않는다. (막히는 지점 3번)
- 템플릿 데이터 항목은 이미지, 문구, 위치, 크기, 색, 화면비 등 편집 상태를 복원할 수 있는 정보를 담는다. 정확한 항목은 확정 후 기록한다. (첫 행동, C18)
- 저장은 생성, 수정, 삭제 즉시 반영한다. 새로고침 후 저장소에서 다시 읽어 확인한다. (C21)
- 이 데이터 구조가 이후 JSON 내보내기, 가져오기 검증의 기준이 된다. 항목명과 필수 항목을 문서로 남긴다.

### 매 작업 점검 체크리스트 (4단계)
- [x] C17 템플릿 3개 이상 생성
- [x] C18 생성한 템플릿 불러오기
- [x] C19 템플릿 수정
- [x] C20 템플릿 삭제
- [x] C21 새로고침 뒤에도 변경 결과 유지
- [x] 수정, 삭제가 ID 기준이며 다른 템플릿에 영향이 없음

### 미정 항목 (사용자 확인 필요)
- [확정] 템플릿 저장: IndexedDB에 이미지 본체까지 저장한다. JSON 내보내기 때는 이미지를 텍스트(Base64 등)로 변환해 담는 방식을 쓴다. (5단계 가져오기 검증과 연결)
- [확정] 삭제 전에 확인 창을 둔다.
- [확정] 템플릿 입력 항목: 이름(최대 40자, 비우면 "템플릿 N" 자동)과 메모(최대 100자). 요구사항에는 입력 항목 명세가 없어서 사용자와 정했다. 나머지(이미지, 문구, 위치, 크기, 색, 화면비)는 현재 편집 상태에서 자동으로 담는다.
- [확정] 템플릿 이미지 저장: 불러온 이미지를 원본 화소 그대로 캔버스에 다시 그려 PNG로 저장한다. EXIF(위치 정보 등)는 사라지고 투명도는 유지된다. 이미지가 없는 상태도 저장할 수 있다.
- [확정] 화면 배치: 오른쪽 도구 영역 아래에 '템플릿' 섹션. 첫 화면의 이미지·문구 도구는 그대로 보인다.
- [확정] 버튼 색(사용자 요청): 모든 버튼(일반, 파일 선택, 화면비, 목록 안의 불러오기·삭제)을 주황으로 한다. css/style.css의 `--accent`(#c2410c)와 `--on-accent`(강조색 바탕 위 흰 글씨)로 정하고, 글씨 대비 4.5 이상을 유지한다. 삭제 버튼도 같은 주황이다.
- [확정] 맨 위 제목 바("밈 카드 스튜디오")는 없앴다(사용자 요청). 화면에는 안 보이고 스크린 리더만 읽는 제목(`h1.visually-hidden`)과 `<title>`만 남겼다. 제목 바가 있던 높이(52px)만큼은 화면 맨 위 여백(css/style.css의 `--top-space`)으로 남겼고, 캔버스 높이 계산에도 이 값을 뺀다.
- [확정] 이미지 올리기 카드(사용자 요청, 참고 화면을 따라 함): 이미지를 불러오기 전에 미리보기 창 안을 픽셀 아트 카드가 채운다(체크무늬 바탕, 주황 픽셀 버튼 "이미지로 시작", "또는 여기에 이미지 끌어다 놓기", "이미지 없이 시작"). 이미지를 불러오거나, 템플릿을 불러오거나, "이미지 없이 시작"을 누르면 닫힌다(화면비를 바꿔도 열려 있다). 카드가 열린 동안 캔버스는 잠긴다(`inert`). 샘플 이미지 줄은 넣지 않았다. 오른쪽 '이미지' 패널은 없앴다(아래 "이미지 패널 제거" 항목). index.html의 `#upload-card`, css/style.css의 `.upload`, `.pixel-btn`, js/app.js의 `setUploadOpen()`.
- [확정] 기본 템플릿(사용자 요청): assets/templates의 이미지 3장으로 만든 템플릿 3개(기본 템플릿 1~3, 화면비 1:1, 4:5, 9:16, id default-template-1~3)를 열 때마다 저장소에 없는 것만 채운다(수정한 것은 그대로, 모두 있으면 이미지를 받지 않는다). 수정할 수 있지만 **삭제 버튼이 없고 삭제도 막는다**(기본 템플릿 3개만. 사용자가 만든 템플릿은 삭제할 수 있어 T03-C20은 그대로 충족한다). 예전에 지웠거나 저장소가 비었으면 다음에 열 때 다시 채운다. 세 이미지는 넓은 배경 가운데에 캐릭터가 있어서 어느 화면비로 바꿔도 캐릭터가 잘리지 않는다(가장 빠듯한 경우도 가장자리 여유 69px 이상). 기본 템플릿의 문구와 위치도 아래 "기본 문구와 크기" 항목의 기본값을 쓴다(위치 y 0.7). 코드는 js/app.js의 `ensureDefaultTemplates()`.
- [확정] 템플릿 목록의 카드 그림(사용자 요청, 기본 템플릿이 글자로만 설명되어서): 목록의 모든 템플릿 항목 왼쪽에 그 템플릿의 카드 모양(화면비대로 꽉 채워 중앙을 자른 모습, 글자는 없음)을 높이 88px 그림으로 보여 준다. 이미지가 없는 템플릿은 흰 카드다. 그림은 메모리에서 만들어 두고(같은 템플릿은 다시 만들지 않음), 없어지거나 바뀐 템플릿의 그림은 지운다. 저장소나 JSON에는 넣지 않는다. 코드는 js/app.js의 `thumbFor()`.
- [확정] 화면 축소(사용자 요청, 화면이 너무 꽉 차 보여서): 큰 화면(폭 1200px 이상, 높이 560px 이상)에서 배치 전체를 `--ui-scale`(0.85)배로 줄여 가운데에 둔다. 배치 계산은 창을 꽉 채우는 값 그대로라 스크롤 조건은 같다. 줄어든 만큼 작은 글씨(안내, 설명, 목록 정보)는 키워서 보이는 크기가 11.5px 이상이 되게 했다. 비율을 바꾸려면 `--ui-scale` 값 하나만 고친다(1이면 이전처럼 꽉 채움).
- [확정] 남색 배경은 모두 흰색(사용자 요청): 화면은 맥이 어두운 모드여도 항상 밝은 화면이다(어두운 모드 화면은 없앴고 `color-scheme: light`로 고정). 카드의 기본 배경(이미지를 불러오지 않았을 때)도 흰색이고, 기본 글자색은 #222222이다. 예전에 저장한 템플릿 중 이미지가 없고 글자색이 흰색인 것은 흰 배경 위에서 안 보인다.
- [확정] 큰 화면 배치(사용자 요청): 1920×1080, 1366×768에서 스크롤 없이 한 페이지에 다 보인다. 창 폭 1200px 이상, 높이 560px 이상이면 오른쪽 도구를 두 열(문구 / 템플릿)로 나누고 캔버스 크기를 창 높이에 맞춘다. 템플릿이 많아지면 목록 안에서만 스크롤된다. 그보다 작은 화면(태블릿, 휴대폰)은 기존 배치(스크롤 있음)이며 요구사항 범위 밖이다. 기본 상태 기준 창 높이 560px, 긴 문구 알림이 뜨면 600px 이상에서 스크롤이 없다.
- [확정] 이미지 패널 제거(사용자 요청, "이미지 불러오기 없어도 괜찮을 것 같아"→ 문구 창 위의 '이미지' 패널 전체): 오른쪽 도구 영역에는 '문구'와 '템플릿'만 남는다. 이미지 넣기는 미리보기 창의 올리기 카드(처음 화면, 끌어다 놓기 포함)와, 이미지가 있을 때 창 오른쪽 위에 뜨는 "이미지 바꾸기" 버튼(이미지 없이 시작한 경우 "이미지 넣기")으로 한다. 버튼을 누르면 카드가 다시 열리고 "이미지 없이 시작" 자리가 "돌아가기"로 바뀐다. 파일 입력(`#file-input`)은 숨겨 두고 카드의 "이미지로 시작"이 연다. 불러오기 안내·거부 이유(`#message`)는 미리보기 아래 `.notes` 줄로 옮겼고(이미지 이름 줄 `#image-name`은 사용자 요청으로 없앴다. 예전의 "파일이름 (가로×세로, 형식)", "템플릿에서 복원" 문구는 더 이상 나오지 않는다), 내려받기 안내(`#export-message`)도 같은 줄에 나온다. 캔버스 높이 계산은 이 줄 높이(`--notes-h`, 66px)를 뺀다. 요구사항 해석: C03의 "이미지 편집 도구"는 첫 화면의 올리기 카드(이미지로 시작)와 "이미지 바꾸기"로 본다. C09, C10은 이 카드에서 잘못된 파일을 넣어 확인한다(카드는 열린 채 이유가 미리보기 아래에 뜨고 기존 작업은 그대로). `.file-btn` CSS는 템플릿의 "JSON 가져오기"가 그대로 쓴다.
- [확정] 기본 문구와 크기(사용자 요청): 기본 문구는 "문구를 끌어서 위치를 옮기세요."와 "화살표 키로도 옮길 수 있습니다." 두 줄이고, 기본 크기는 26(1080 기준 px)이다. 예전 화면비 줄의 안내 문장은 없앴다. 처음에 기본 크기를 16으로 요청받아 슬라이더 최소를 24에서 16으로 넓혔고, 기본을 26으로 바꾼 뒤에도 범위는 16~240으로 뒀다(자동 축소의 최소 크기 `MIN_FIT_SIZE` 16과 같다). 저장과 가져오기 검증도 같은 범위(16~240)를 쓴다. 기본 템플릿 3개도 같은 문구와 크기로 새로 만들어진다(이미 저장된 것은 그대로).
  - 기본 위치와 이미지를 넣을 때(사용자 요청, 기본 문구가 처음 이후로 계속 바뀌어서): 기본 위치는 x 0.5, y 0.7(가운데에서 아래쪽)이다. 이미지를 넣거나 "이미지 바꾸기"로 바꿀 때마다 문구, x, y를 기본값으로 되돌린다(직접 쓴 문구도 알림 없이 교체된다는 것을 사용자가 정했다. 크기와 색은 그대로 둔다). 실패한 파일에서는 아무것도 바꾸지 않는다(C09). 템플릿을 불러올 때는 C18대로 저장한 문구, 위치, 크기, 색을 그대로 복원한다(기본값으로 덮어쓰지 않는다). 기본값은 js/app.js의 `DEFAULT_TEXT`, `DEFAULT_SIZE`, `DEFAULT_X`, `DEFAULT_Y`.
  - 기본 템플릿 목록 표시(사용자 요청): 기본 템플릿 1~3은 목록에서 이름, 설명(메모), 화면비·이미지 여부·시각 줄, "불러오기" 버튼을 모두 빼고 카드 그림만 보여 준다(이미지가 이름, 문구, 화면비를 이미 보여 준다는 이유). 그림 자체가 불러오기 버튼이다(`button.tpl-thumb`, 마우스와 키보드 Enter/Space).
  - 기본 템플릿 위치(사용자 요청): 기본 템플릿 그림 3개는 오른쪽 템플릿 패널이 아니라 **미리보기 창 바로 바깥 왼쪽 여백에 위에서부터 세로로** 놓는다(index.html의 `ul#tpl-defaults`, 창 `.canvas-wrap` 안의 절대 위치, css/style.css의 `.tpl-defaults`). 폭과 그림 크기는 왼쪽 캐릭터(`.deco-left`)와 같은 여백(`--space`)에 맞춘다. **왼쪽 배경 공룡(assets/left.png)은 움직이거나 바꾸지 않는다**: 공룡은 창 아래쪽 끝에 서 있고 기본 템플릿은 위쪽에 있어 서로 겹치지 않으며, 위치와 크기는 목록과 무관하다. 창 폭 1280px 미만(공룡을 숨기는 화면)에서는 기본 템플릿을 창 안쪽 위에 가로 한 줄로 놓는다. 오른쪽 템플릿 목록(`#tpl-list`)에는 직접 만든 템플릿만 나오고, 없으면 "저장한 템플릿이 없습니다."만 보인다. 스크린 리더에는 "기본 템플릿 N 불러오기"로 읽힌다. 사용자가 만든 템플릿은 이름, 메모, 정보 줄, 불러오기·삭제 버튼이 그대로다. 저장 데이터의 name, memo는 그대로 두었다(JSON 내보내기에 들어간다). 삭제 막기는 그대로다.
  - 이미 저장된 기본 템플릿 맞춤(사용자 요청, "문구랑 위치 변경해줘"): 열 때마다 `syncDefaultTemplates()`가 기본 템플릿 1~3 중 **수정 저장하지 않은 것**(고친 시각 = 만든 시각)의 문구, 크기, 위치를 현재 기본값으로 맞춘다. 수정 저장한 기본 템플릿과 사용자가 만든 템플릿은 건드리지 않는다. 고친 시각은 바꾸지 않아서 앞으로 기본값이 바뀌어도 계속 따라간다.
- [확정] 이미지 전 문구 잠금(사용자 요청, 요구사항 추가): 이미지를 넣기 전(카드가 처음 열려 있는 상태)에는 **문구 도구 전부**(내용, 크기 슬라이더, +/- 버튼, 색)가 비활성이고 캔버스도 잠겨서(끌기, 방향키 불가) 문구를 입력하거나 고칠 수 없다. 잠금 안내는 입력칸 안의 "이미지를 넣어주세요." 글씨로 한다(아래 "입력 불가 안내" 항목). 문구 도구 위의 별도 안내 문장(처음엔 "이미지를 먼저 넣으면 문구를 고칠 수 있습니다.", 이어서 "문구를 끌어서 위치를 옮기세요. / 화살표 키로도 옮길 수 있습니다.")은 사용자 요청으로 없앴다. 잠금은 `fieldset#text-fields`의 `disabled`(js/app.js의 `updateTextLock()`)로 한다. 이미지를 넣거나, **"이미지 없이 시작"을 누르면(흰 카드가 이미지 자리를 대신한다, `state.blank`)**, 또는 템플릿을 불러오면(이미지 없는 템플릿은 흰 카드) 잠금이 풀린다. 흰 카드에서 "이미지 넣기"로 카드를 다시 열어도 도구는 열려 있다. 잘못된 파일을 넣으면 잠금과 카드는 그대로이고 이유가 미리보기 아래에 뜬다(C09, C10). 새로고침하면 다시 잠긴 상태로 시작한다. 요구사항 해석: C03의 "문구 편집 도구가 첫 화면에 보인다"는 잠긴 채로 보이는 것을 충족으로 본다.
- [확정] 문구 크기(사용자 요청, 요구사항 추가): 조작 범위는 16~100이다(슬라이더 최대 100). 크기 숫자 옆에 -와 + 버튼이 있고 한 번에 1씩 바꾸며, 16과 100에서 각각 멈추고 버튼이 비활성이 된다(`setSize()`, 슬라이더와 같은 곳으로 바꾼다). **저장 데이터가 허용하는 범위는 예전 그대로 16~240**이다(`SIZE_DATA_MAX`): 예전에 100보다 크게 저장한 템플릿과 JSON(samples/import-tests/valid.json의 크기 120)을 계속 읽을 수 있고, C22~C24 결과가 바뀌지 않는다. 100보다 크게 저장된 템플릿을 불러오면 화면에서는 100으로 줄여 불러오고 그 사실을 알린다(저장소의 값은 그대로이며 다시 저장하면 100이 저장된다).
- [확정] 문구 입력칸 아래와 크기 줄(사용자 요청): (1) 입력칸 아래의 "n / 200 (200자까지 입력할 수 있습니다)" 줄을 없앴다. 긴 문구 알림(`#fit-note`)은 3단계 요구(글자를 조용히 잘라 내지 않는다)에 필요해서 그대로 뒀다. (2) 크기 줄은 "크기 26 [-] [+]"(-, + 버튼이 크기 숫자 바로 옆, 왼쪽)와 **오른쪽 끝의 "문구 지우기" 버튼**으로 되어 있다. 문구 지우기는 문구 내용만 비우고(`state.text = ""`) 크기, 색, 위치는 그대로 두며, 입력칸에 포커스를 준다. 빈 문구는 카드에 글자를 그리지 않는다(3단계 검사의 빈 문구와 같다). 문구가 비어 있거나 이미지 전 잠금 중이면 비활성이다. 확인 창은 없다.
- [확정] 문구 글자 제한(사용자 요청, 요구사항 추가): 입력칸은 **50자**까지만 입력된다(`maxlength`). 저장 데이터가 허용하는 문구 길이는 예전 그대로 0~200자(`TEXT_MAX`)라서, 예전에 50자보다 길게 저장한 템플릿과 JSON도 읽히고, 불러올 때 글자를 잘라 내지 않는다(새로 입력하는 것만 50자로 막힌다).
- [확정] "이미지 바꾸기"/"이미지 넣기" 버튼(사용자 요청): 버튼을 작게(글씨 13px, 화면에서 약 71×31px. 처음엔 높이 17px이었고 사용자 요청으로 21px, 다시 1.5배인 31px로 세로를 늘렸다) 하고, **미리보기 창 위쪽 회색 띠**(`--wrap-top`, 58px. 버튼은 창 위 끝에서 약 10px, 이미지와는 약 5px 띄우고, 가로 위치는 **맨 오른쪽 버튼("내려받기")의 오른쪽 끝을 창 오른쪽 끝에서 왼쪽의 화면비 1:1 버튼과 같은 여백(테두리 바깥 기준 약 18px, 화면에서 15.3px)만큼 띄우는 것**이다(사용자 요청 "1:1 버튼 왼쪽 공백만큼 내려받기 오른쪽 공백을 동일하게", 화면비와 화면 크기에 상관없이 고정, `.strip-actions`의 `right: 20px`). (변경 이력: 처음엔 1:1 이미지 옆 회색 띠 기준(가운데, 다시 40%)으로 정했다가 이 규칙으로 바뀌어 `--sq` 계산은 없앴다.) 사용자 요청으로 위쪽 간격을 5px에서 10px로 넓혔다). 같은 높이의 띠가 **아래에도** 있어서 이미지가 회색 창의 사방 가운데에 온다(사용자 요청: 위아래 띠 같음, 좌우도 가운데)의 오른쪽에 둔다. 1:1은 이미지가 창 폭을 거의 꽉 채워서 옆 회색이 12px뿐이라, 어느 화면비에서도 이미지를 침범하지 않게 창 위쪽에 띠를 만들었다. **회색 창 크기는 화면비와 이미지 유무에 관계없이 같다**(사용자 요청, "이미지 넣기 전 화면과 넣은 뒤 화면의 크기를 같게"): 창 높이는 화면 높이에서 정하고(`.canvas-wrap`의 `height`), 이미지 넣기 전의 올리기 카드가 이 창 전체를 덮는다(`.upload`의 `inset: 4px` + 픽셀 테두리 4px). 이미지는 창 안에서 위아래 띠를 뺀 높이에 맞춰 가운데에 온다. 1366×768 기준 창은 약 456×520px, 1:1 이미지는 422px, 1920×1080에서는 창 762×785px, 이미지 687px이다(도구줄을 없앤 만큼 창이 세로로 커졌다). 배경 공룡(left.png)과 왼쪽 기본 템플릿의 위치는 그대로다.
- [확정] 화면비 선택 위치(사용자 요청): "화면비 1:1 4:5 9:16"을 도구줄에서 **미리보기 회색 창 안 위쪽 띠의 왼쪽**으로 옮겼다(index.html의 `.canvas-wrap > fieldset.ratio`, 창 왼쪽 끝에서 약 10px, "이미지 바꾸기" 버튼과 세로 가운데가 같다). 도구줄(`.toolbar`)은 없앴고, "내려받기" 버튼은 아래 "내려받기 버튼 위치" 항목대로 회색 창 안으로 옮겼다. 올리기 카드가 열려 있어도 카드 위(흰 바탕 조각)에서 눌러 화면비를 바꿀 수 있다(`z-index: 3`). 화면비 선택의 동작(캔버스 크기, 저장과 복원, C11~C13)은 그대로다. 모양은 "이미지 바꾸기" 버튼과 같은 **픽셀 버튼**이다(사용자 요청): 흰 바탕, 2px 픽셀 테두리(`--pixel-ink`), 글씨 13px, 높이 31px로 같고, 선택된 화면비는 주황(`--accent`)으로 채운다. 이 화면비 버튼은 주황 테두리 버튼 규칙(버튼 색 항목)의 예외다. "화면비" 글씨는 화면에서 없앴고(스크린 리더용 제목으로만 남김, `legend.visually-hidden`), 그 폭(약 52px)만큼 세 버튼을 가로로 넓혔다(각 40px → 약 57px, 세 버튼 전체 폭은 그대로 208px)(사용자 요청).
- [확정] 내려받기 버튼 글씨(사용자 요청): "PNG로 내려받기"를 "내려받기"로 바꿨다. 파일 형식은 그대로 PNG이고 동작도 같다(같은 render()로 새 캔버스에 다시 그려 저장, 파일 이름은 meme-card-화면비-시각.png).
- [확정] 내려받기 버튼 위치(사용자 요청): "내려받기" 버튼을 도구줄(화면비 줄이 있던 자리, 회색 창 위)에서 **회색 창 안 위쪽 띠로, "이미지 바꾸기/넣기" 버튼의 오른쪽**으로 옮겼다(index.html의 `.strip-actions`, 두 버튼 간격 화면에서 약 5px, 높이 31px로 같다). 그룹의 오른쪽 끝이 위 "이미지 바꾸기/넣기 버튼" 항목의 가로 위치 규칙(화면비 1:1 버튼의 왼쪽 여백과 같게)을 따르고, 그래서 이미지 버튼은 그만큼 왼쪽으로 갔다. 올리기 카드가 열려 있어 이미지 버튼이 숨겨지면 내려받기만 같은 오른쪽 끝에 남는다(다만 이미지를 정하기 전에는 아래 항목대로 내려받기도 숨는다). 도구줄이 없어져 회색 창이 오른쪽 패널 위 끝과 같은 높이에서 시작하고, 그만큼 세로로 커져(캔버스 높이 계산의 여유 86→38px) 이미지도 커졌다(1366×768에서 1:1 이미지 381→422px). 배경 공룡의 위치는 그대로다.
- [확정] 화면비·내려받기 버튼 모양과 표시(사용자 요청): (1) "내려받기" 버튼도 화면비 버튼, "이미지 바꾸기" 버튼과 같은 **픽셀 버튼**(흰 바탕, 2px 픽셀 테두리, 글씨 13px, 높이 31px)으로 바꿨다(더 이상 주황으로 채운 버튼이 아니다). (2) 가로를 넓혔다(약 56px → 78px, `min-width: 92px`). (3) **내려받기 버튼은 이미지 전 문구 잠금과 같은 조건으로 숨긴다**(화면비 선택은 나중에 사용자 요청으로 항상 보이게 바꿨다, 아래 항목): 이미지를 넣기 전(카드가 처음 열려 있는 상태)에는 화면에 보이지 않고(`hidden`, Tab으로도 갈 수 없다), 이미지를 넣거나 "이미지 없이 시작"(흰 카드)을 누르거나 템플릿을 불러오면 나타난다(`updateTextLock()`). 이미지가 있으면 "이미지 바꾸기"로 카드를 다시 열어도 계속 보인다. 새로고침하면 다시 숨겨진 채 시작한다. 그래서 이미지(또는 흰 카드)를 정하기 전에는 화면비를 바꾸거나 내려받을 수 없다. 요구사항 해석: C03은 이미지 편집 도구(올리기)와 문구 도구가 보이면 되므로 충족이고, C11~C13은 이미지를 넣은 뒤 화면비를 골라 확인한다.
- [확정] "내용" 글씨 없앰(사용자 요청, "문구 밑에 내용 텍스트 없애줘"): 문구 도구의 "문구" 제목 바로 아래에 있던 "내용" 글씨(입력칸 이름표)를 화면에서 없앴다. 입력칸은 `aria-label="문구 내용"`으로 스크린 리더에는 이름이 남는다. (앞서 "문구 창 안에 문구 텍스트 아래 내용 없애줘"를 입력칸 아래 "n / 200" 줄로 잘못 읽어 그 줄을 없앴다. 그 줄은 지금도 없다.)
- [확정] 입력 불가 안내(사용자 요청): 이미지가 없어서 문구를 입력할 수 없는 동안(잠금 상태) 입력칸 안에 "이미지를 넣어주세요." 한 줄이 안내 글씨(placeholder)로 보이고(`TEXT_LOCK_MESSAGE`), 입력칸은 비어 있다. 잠금이 풀리면(이미지를 넣거나 흰 카드로 시작하거나 템플릿을 불러오면) 안내가 사라지고 원래 문구(`state.text`)가 나타난다. 잠금이 풀린 뒤 문구를 지워서 비어 있을 때는 이 안내가 뜨지 않는다. (입력칸 위의 "문구를 끌어서 위치를 옮기세요." 안내 문장은 사용자 요청으로 없앴다. 카드의 기본 문구는 그대로 그 문장이다.) 저장하는 문구는 화면 값이 아니라 `state.text`라 잠금 안내가 템플릿에 저장되지 않는다.
- [확정] 템플릿 불러오기 변경(사용자 요청): (1) 템플릿을 불러올 때 "저장하지 않은 편집 내용이 있습니다 … 불러올까요?" 확인 창을 없앴다(불러오기가 편집을 잃게 하지 않으므로). 삭제 확인 창은 그대로다. (2) **불러오면 이미지와 화면비만 템플릿 것으로 바뀌고, 문구(내용, 크기, 색, 위치)는 지금 편집 중인 것을 그대로 유지한다.** 기본 템플릿(왼쪽 그림)과 직접 만든 템플릿 모두에 해당한다(사용자가 "모든 템플릿"으로 골랐다). 템플릿에 저장된 문구 등은 그대로 남고 화면에는 적용되지 않으며, "선택한 템플릿 수정 저장"을 누르면 지금 문구로 저장된다. 불러오면 안내에 "문구는 지금 것을 그대로 유지했습니다"가 붙는다. **요구사항 영향: T03-C18("저장한 편집 내용이 복원되는지 확인")의 문구 부분은 충족하지 않는다.** 사용자가 이 충돌을 알고 정했다. 이미지와 화면비의 복원은 그대로다. 이 변경으로 "저장하지 않은 편집" 추적(`dirty`)은 필요 없어져 코드에서 지웠다.
- [확정] 내려받기 버튼 표시 조건(사용자 요청, 앞의 "화면비·내려받기 버튼 모양과 표시" 항목을 이어서): **"이미지 바꾸기/넣기" 버튼이 보이지 않으면 "내려받기" 버튼도 보이지 않는다.** 그래서 올리기 카드가 열려 있는 동안(처음 화면, "이미지 바꾸기"를 눌러 카드를 다시 연 상태, 잘못된 파일을 넣어 카드가 그대로인 상태)에는 내려받기가 숨고, "돌아가기"나 이미지를 넣어 카드가 닫히면 다시 나타난다(`setUploadOpen()`에서 `changeImageBtn.hidden`과 `downloadBtn.hidden`을 같이 정한다). 화면비 선택은 이 조건과 무관하게 항상 보인다(다음 항목).
- [확정] 화면비 선택은 항상 보인다(사용자 요청): 처음 화면(이미지 전)과 카드가 열려 있는 동안에도 화면비 1:1 / 4:5 / 9:16이 회색 창 안 위쪽 띠에 보이고 눌러서 바꿀 수 있다. 이미지 전에 화면비를 바꿔도 올리기 카드와 회색 창의 크기는 그대로다(창 크기는 화면비와 무관). 이 항목이 앞의 "화면비 선택이 이미지 전에는 숨는다"는 설명을 대체한다.

## 5단계: JSON 복원, 완성 이미지 3개, 메타데이터·개인정보·비밀값 점검, 제출 문서

### 하는 일
정상 JSON과 잘못된 JSON을 시험하고, 완성 이미지 3개와 사용 권한을 확인한다.

### 완료 목표
안전한 복원, 내보내기, 완성본, 메타데이터 제거를 마친다.

### 첫 행동
1. 정상 JSON, 손상 JSON, 필수 항목 누락 JSON을 각각 가져온다.
2. 완성 이미지 3개를 열고 권한, 위치 정보, 비밀값을 확인한다.

### 통과 기준

#### A. JSON 가져오기 (복원)
| ID | 기준 | 검증 방법 |
|---|---|---|
| T03-C22 | 정상 JSON을 가져오면 템플릿이 복원된다. | 가져오기 후 템플릿 내용이 원본과 일치하는지 대조 |
| T03-C23 | 문법이 손상된 JSON은 저장 전에 거부되고 기존 템플릿이 유지된다. | 가져오기 전후 템플릿 건수와 내용 대조 |
| T03-C24 | 필수 항목이 빠진 JSON은 저장 전에 거부되고 기존 템플릿이 유지된다. | 가져오기 전후 템플릿 건수와 내용 대조 |

#### B. 완성 이미지 3개
| ID | 기준 |
|---|---|
| T03-C25 | 서로 다른 문구 또는 비율을 가진 완성 이미지가 3개 있다. |
| T03-C26 | 완성 이미지 3개가 각각 정상적으로 열린다. |
| T03-C27 | 이미지 3개 각각에 출처가 기록되어 있다. 본인 제작이면 "본인 제작"으로 기록한다. 본인 제작이 아니면 원본 출처 URL과 사용 허가 근거(라이선스명, 이용약관, 수업 제공 표기 중 하나)를 기록한다. |

#### C. 메타데이터, 개인정보, 비밀값
| ID | 기준 |
|---|---|
| T03-C28 | 공개 이미지의 위치 정보 메타데이터가 0건이다. |
| T03-C29 | 공개 화면과 제출물의 개인정보가 0건이다. |
| T03-C30 | 공개 화면, 배포 파일, 제출물의 비밀값 원문(API 키, 토큰 등)이 0건이다. |

#### D. 문서
| ID | 기준 |
|---|---|
| T03-C31 | (사용자가 직접 작성) 짧은 확인 방법에 네 가지가 구분되어 있다. ① 어디로 가나요 ② 3단계 이내로 무엇을 하나요 ③ 무엇이 보이면 통과인가요 ④ 안 될 때 무엇이 보이나요 |
| T03-C32 | (사용자가 직접 작성) 제출문에 세 가지가 구분되어 있다. ① AI에게 맡긴 일 ② 학생이 직접 판단한 일 ③ AI 제안을 따르지 않은 일(없으면 없었던 이유) |

### 막히는 지점과 확인 순서
- 문제: 잘못된 JSON을 가져온 뒤 기존 목록이 사라진다.
- 확인 순서: 전체 검증 후 저장 -> 오류 시 변경 금지 -> 가져오기 전후 건수 대조

### 남길 것 (산출물)
- JSON 세 종류(정상, 문법 손상, 필수 누락)의 가져오기 결과
- 완성 이미지 3개

### 설계 제약 (통과 기준에서 도출)
- 가져오기는 "파싱 -> 구조 검증 -> 저장" 순서로 한다. 어느 단계든 실패하면 저장소에 쓰기를 하지 않는다. 검증은 파일 전체를 끝까지 마친 뒤 저장한다. (C23, C24)
- 문법 오류와 필수 항목 누락은 구분해서 이유를 화면에 표시한다. (C23, C24, C31의 ④)
- 필수 항목 정의는 4단계의 템플릿 데이터 구조를 기준으로 한다. JSON 내보내기 형식과 가져오기 검증이 같은 정의를 쓴다. (C22, C24, 완료 목표의 "내보내기")
- 가져오기 전후 템플릿 건수를 대조해 결과를 남긴다. (막히는 지점)
- 내보내는 이미지에서 위치 정보 등 EXIF 메타데이터를 제거한다. 완성 이미지 3개의 실제 파일을 검사 도구로 열어 0건을 확인한다. (C28)
- 완성 이미지 3개는 문구 또는 화면비가 서로 달라야 한다. 각각 실제로 열어 확인한다. (C25, C26)
- 이미지별 출처 기록을 남긴다. 이미지, 폰트 등 외부 자산은 출처와 허가 근거 없이 넣지 않는다. (C27)
- 코드, 배포 파일, 제출물에 비밀값 원문을 넣지 않는다. 제출 전에 저장소 전체를 검색해 확인한다. (C30)
- 개인정보(이름, 이메일, 사진 속 얼굴, 경로에 든 사용자명 등)가 화면, 이미지, 제출물에 들어가지 않게 한다. 실제 개인 이메일이나 로컬 경로가 문서와 이미지에 남았는지 확인한다. (C29)
- 짧은 확인 방법(C31)과 제출문(C32)은 사용자가 마지막에 직접 작성한다. 사용자가 요청하면 네 가지, 세 가지 구분이 갖춰졌는지 점검만 돕는다.

### 매 작업 점검 체크리스트 (5단계)
- [x] C22 정상 JSON 복원
- [x] C23 손상 JSON 거부, 기존 템플릿 유지
- [x] C24 필수 누락 JSON 거부, 기존 템플릿 유지
- [x] C25 서로 다른 문구 또는 비율의 완성 이미지 3개
- [x] C26 이미지 3개 모두 열림
- [x] C27 이미지별 출처, 허가 근거 기록
- [x] C28 위치 메타데이터 0건
- [ ] C29 개인정보 0건 (공개 화면, 제출물) (사용자 담당, Claude는 판정하지 않음)
- [ ] C30 비밀값 원문 0건 (공개 화면, 배포 파일, 제출물) (사용자 담당, Claude는 판정하지 않음)
- [ ] C31 짧은 확인 방법 네 요소 (사용자 담당, Claude는 작성하지 않음)
- [ ] C32 제출문 세 요소 (사용자 담당, Claude는 작성하지 않음)
- [x] 가져오기 전후 건수 대조 결과와 JSON 세 종류 결과를 남김

### 미정 항목 (사용자 확인 필요)
- [확정] 출처 기록 위치: CREDITS.md (사용자가 위치를 따로 지정하지 않아 제안안을 기본값으로 사용. 바꾸려면 알려 달라고 한다.)
- [확정] 완성 이미지 3개: PNG(1080 크기 3종 중), 저장 위치는 samples/final/. 이미지별 출처는 CREDITS.md에 적는다.
- [확정] assets/mouse-cursor.png, assets/left.png의 출처: 본인이 ChatGPT로 직접 생성(AI 생성). 허가 근거는 OpenAI 이용약관. CREDITS.md에 기록했다.
- [미정] "제출물"의 범위: 명세에 정의가 없다. 잠정 해석은 배포된 서비스 + 공개 소스 저장소 + 완성 이미지 3개 + 확인 방법과 제출문. C29, C30은 저장소 전체, 배포 결과물, 완성 이미지, 제출 문서를 모두 점검한다. 사용자가 실제 제출 방식을 알려 주면 갱신한다.
- [해소] 짧은 확인 방법과 제출문은 사용자가 마지막에 직접 작성한다. Claude가 파일명과 위치를 정하지 않는다.
- [확정] JSON 내보내기와 가져오기: 템플릿 섹션에 버튼을 둔다. 파일명은 meme-card-templates-YYYYMMDD-HHMMSS.json.
- [확정] 가져오기 오류 처리: 명세(설계 제약)대로 하나라도 문제가 있으면 전부 저장하지 않는다. 문법 오류와 필수 항목 누락은 구분하고, 몇 번째 항목의 어느 항목이 왜 문제인지 모두 나열해 알려 준다. 가져오기 전후 건수를 함께 표시한다.
- [확정] 가져오기 id: 파일의 id를 유지하고, 이미 있는 id와 겹칠 때만 새 id로 추가한다. 기존 템플릿은 덮어쓰지 않는다.
